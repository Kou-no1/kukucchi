# 掛け算を遊びながら習得するWEBアプリ「くくっち」開発指示書 v2

あなたは、子ども向け教育WEBアプリの設計・実装・テストを担当する、シニアフルスタックエンジニア兼UI/UXデザイナーです。

8歳以下の子どもが、勉強をしている感覚を持たずに繰り返し遊び、気づいたときには掛け算九九を習得しているWEBアプリを開発してください。

まず既存リポジトリのファイル構成、設定、依存関係を確認してください。

次に、この指示書を `docs/SPEC.md` としてリポジトリに保存し、リポジトリ直下に `AGENTS.md` を作成して以下を記載してください。

- `docs/SPEC.md` の仕様に従って開発すること
- 現在の進行フェーズ（Phase完了ごとに更新すること）
- 各Phase完了時に lint・test・build をすべて成功させてから git commit すること

実装前に以下を簡潔に整理してください。

1. 現在のプロジェクト構成
2. 実装する機能一覧
3. 主要コンポーネント
4. データ構造
5. 実装順序
6. 想定される技術的な問題

整理後は確認を待たず、そのまま実装を開始してください。

ただし、今回のセッションで実装するのは Phase 1〜2（遊べるMVP）までです。Phase 2完了時点で停止し、26章の形式で報告してください。Phase 3以降は、人間によるレビュー後に別途指示します。

---

# 1. プロダクトの目的

## メインターゲット

- 主な対象は小学1〜3年生、8歳以下
- タブレット、スマートフォン、PCで利用
- 文字が十分に読めない子どもでも操作できる
- 1回30秒〜3分程度で遊べる
- 繰り返し遊びたくなる
- 掛け算が苦手な子どもを責めない
- 他人との競争より、過去の自分からの成長を重視する

## 高学年向け追加ターゲット

小学5・6年生および中学受験を目指す児童向けに、発展計算モードも実装します。

---

# 2. 開発方針

## 技術構成

既存プロジェクトに明確な構成がなければ、以下を採用してください。

- React
- TypeScript
- Vite
- CSS Modulesまたは通常のCSS
- React Router
- localStorage
- Vitest
- React Testing Library
- ESLint
- Prettier

外部UIライブラリは、必要性が高い場合を除いて使用しないでください。

初期バージョンではバックエンドを使用せず、すべての進捗をlocalStorageへ保存してください。

ただし、将来的にFirebaseまたはSupabaseへ移行しやすいように、保存処理はRepositoryまたはService層へ分離してください。

## ルーティングと公開環境

- GitHub Pagesへ公開可能な構成にしてください。
- React Routerは必ず **HashRouter** を使用してください。BrowserRouterはGitHub Pagesでリロード時や直リンク時に404になるため、使用しないでください。
- Viteの `base` は `/リポジトリ名/` に設定してください（実際のリポジトリ名に合わせること）。

## 対応環境

- Chrome
- Edge
- Safari
- iPad
- Androidタブレット
- スマートフォン
- PC

児童1人1台の学校端末（GIGAスクール端末）と、家庭の端末の両方での利用を想定します。

## ドキュメントとコミット運用

- この指示書は `docs/SPEC.md` として保存し、仕様判断に迷ったら参照してください。
- 各Phase完了時に `npm run lint` / `npm run test` / `npm run build` をすべて成功させ、Phase番号を含むコミットメッセージで git commit してください。

---

# 3. デザインコンセプト

## 全体イメージ

- 明るい
- 楽しい
- かわいい
- 子どもっぽすぎない
- ゲーム感がある
- 大きなボタン
- 少ない文字
- 直感的な操作
- 視線移動が少ない
- 画面ごとの目的が明確

## UI要件

- タップ領域は最低44px以上
- 主要ボタンはさらに大きくする
- 小さな文字を使わない
- ひらがな中心
- 難しい漢字にはふりがな、またはひらがな表記
- 回答ボタンは片手でも押しやすい
- 正解時は気持ちよいアニメーションを表示
- 不正解時に強い警告音や大きな赤いバツを出さない
- 不正解時は「おしい！」「もういちど！」などの表現にする
- 音を消せる設定を用意する
- アニメーションを減らせる設定を用意する
- 色だけで正解・不正解を区別しない
- キーボード操作にも対応する

## レスポンシブ設計

特に以下の画面幅を確認してください。

- 360px
- 390px
- 768px
- 1024px
- 1440px

---

# 4. 世界観

仮タイトルは「くくっち」とします。

掛け算を解くことで、不思議な生き物「くくっち」が育つ世界観にしてください。

## 基本設定

- 最初は卵から始まる
- 問題に正解すると経験値とコインを獲得する
- 一定レベルで進化する
- コインで服、帽子、部屋の家具などを購入できる
- 学習しなかったことでキャラクターが病気になったり弱ったりしない
- 久しぶりにアクセスした場合も責めない
- 「またあえてうれしい！」のように歓迎する

初期実装では、外部画像素材に依存しすぎないでください。

CSS、絵文字、SVG、簡単な図形などを使い、あとから正式なキャラクター画像へ差し替えられる構造にしてください。

---

# 5. 必須画面

以下の画面を実装してください。

## 5.1 初回設定画面

入力項目：

- ニックネーム
- アイコン選択
- 音のON・OFF

学習レベルの選択は初回設定に表示しません。保存データ上の `learningLevel` は互換性のため保持し、既定値を設定してください。

個人情報保護の観点から、氏名、メールアドレス、生年月日は要求しないでください。

利用は1端末1ユーザーを前提とします。プロフィール切替や複数セーブスロットは実装しないでください。

## 5.2 ホーム画面

表示内容：

- 「ほしをえらぶ」を主役にした星選択
- たしざんのほし（live）
- ひきざんのほし（live）
- かけざんのほし（live）
- プレイヤーアイコンと最小限のステータス
- カスタム
- 図かん
- ショップ
- せってい

ホーム画面上部に情報を詰め込みすぎないでください。UFO窓、詳細なLv/EXP/コイン、今日のミッション、にがては、星を選んだ後の星メニュー側で見られるようにしてください。

## 5.3 星メニュー画面

ホームで星を選んだ後、その星のメニューを表示してください。

- かけざんのほし: あそぶ、おぼえる、スピード、高学年、にがてもんすたー、ぼすばとる、ろけっと、たからばこ。
- たしざんのほし: おぼえる、あそぶ、スピード、たしざんボスをliveにする。
- ひきざんのほし: おぼえる、あそぶ、スピード、ひきざんボスをliveにする。
- 高学年（平方数・円周率）は、かけざんのほしメニューの中に置く。
- 旧 `/games` 導線は互換用に残してもよいが、星メニューへ集約し、ホームから独立したモード一覧としては出さない。

## 5.4 リザルト画面

表示内容：

- 正解数
- 問題数
- 正答率
- 平均回答時間
- 最大コンボ
- 獲得コイン
- 獲得経験値
- 次のレベルまでのEXPバー
- モード別の追加結果
- 自己ベスト更新
- 新しく獲得した称号
- 苦手になった問題
- 習得した問題
- もう一回
- ホームへ戻る

---

# 6. ゲームモード

## 6.1 おぼえるモード

掛け算をまだ覚えていない子ども向けです。

### 学習段階

1. 見る
2. 聞く
3. まねして読む
4. 答えを選ぶ
5. 自分で答える

### 音声の実装方法

- 「聞く」はWeb Speech API（speechSynthesis）による読み上げを使用してください。日本語ボイスが利用できない端末では、読み仮名のテキスト表示にフォールバックし、段階として成立させてください。
- 「まねして読む」は音声認識を使いません。読み仮名を表示し、子どもが声に出して読んだら「よめた！」ボタンを押す自己申告式にしてください。SpeechRecognition APIおよびマイクは使用禁止です。

### 表示例

`3 × 4`

と同時に、3個ずつのまとまりが4組あることを視覚表示してください。

例：

```text
●●●
●●●
●●●
●●●
```

以下の切り替えを用意してください。

- まとまり表示
- 数直線表示
- たし算表示
- 九九の読み方
- 答えを隠す

### 出題方式

- 段ごと
- 順番
- 逆順
- ランダム
- 4択
- 答え入力
- 式を選ぶ逆問題

## 6.2 スピードモード

以下を実装してください。

- 30秒チャレンジ
- 60秒チャレンジ
- 10問タイムアタック
- 20問タイムアタック
- ノーミスチャレンジ
- 段限定チャレンジ
- 全九九チャレンジ

### スコア計算

基本仕様：

- 正解1問：100点
- 回答速度ボーナス：最大50点
- 5コンボごとにボーナス倍率上昇
- 誤答時はコンボ解除
- ノーミスの場合は最終スコアにボーナス
- むやみに高速連打しても得をしない設計

問題表示から400ms未満で押された回答は、速度ボーナスの対象外にしてください（高速連打対策）。

## 6.3 ランダムゲームモード

最低3種類のミニゲームを実装してください。

### A. モンスターバトル

- 正解すると攻撃
- 連続正解で必殺技ゲージ上昇
- 不正解でも即敗北にしない
- 10問程度で終了
- 最後に宝箱を獲得

### B. 宝箱チャレンジ

- 3問正解すると鍵を1本獲得
- 鍵を集めて宝箱を開ける
- 宝箱からコインまたはアイテムを獲得

### C. ロケットチャレンジ

- 正解すると燃料がたまる
- 回答速度と正確さで到達地点が変わる
- 月、火星、木星などを目指す

ゲームロジックと見た目を分離し、あとから別のゲームを追加しやすくしてください。

## 6.4 にがてモンスターモード

間違えた式を「にがてモンスター」として登録してください。回答に時間がかかっただけの正解は、子どもに見える「にがて」ラベルには登録しません。

回答時間は裏方の出題優先度や習熟度4→5判定には使います。

例：

- 7×8モンスター
- 6×7モンスター
- 8×9モンスター

### 克服条件

- 合計3回正解する
- そのうち1回は登録日と別の日に正解する
- 回答速度は克服条件に含めない

克服するとモンスターが仲間になり、図鑑へ登録されます。

一度の連続正解だけで完全習得とは判定しないでください。

## 6.5 ボスバトル

各段を習得したあとに解放してください。

例：

- 1・2の段ボス
- 3の段ボス
- 4の段ボス
- 全九九ボス

1の段は難度が低いため、独立ボスにせず2の段ボスに統合してください。

ボス戦は10〜15問で構成してください。

不正解でも最初からやり直しにせず、ヒントや再挑戦の機会を与えてください。

---

# 7. 高学年・中学受験モード

低学年向け画面とは明確に分離してください。

名称は「スーパー計算モード」とします。

## 7.1 平方数（逆問題を含む）

以下を出題してください。

- 11×11
- 12×12
- 13×13
- 14×14
- 15×15
- 16×16
- 17×17
- 18×18
- 19×19
- 20×20
- 必要に応じて25×25まで

### 逆問題

√（根号）は中学3年の学習内容のため、本アプリでは一切使用しません。逆問題は以下の2形式で実装してください。

形式1（□を求める）：

```text
□×□＝144
□に入る数はどれ？
11　12　13　14
```

形式2（式を選ぶ）：

```text
144になるのはどれ？
12×12
13×13
14×14
15×15
```

## 7.2 3.14計算

以下の計算を練習できるようにしてください。

- 3.14×整数
- 整数×3.14
- 円周
- 円の面積
- 半径と直径の区別
- 頻出結果の暗算

初期出題範囲：

- 3.14×1〜20
- 3.14×25
- 3.14×50
- 3.14×100

回答が小数になる入力式問題では、13章の小数点キー付きテンキーを使用してください。初期実装は4択中心で構いません。

## 7.3 発展計算

以下のカテゴリを用意してください。

- 2桁×1桁
- 2桁×2桁
- 25を使う計算
- 125を使う計算
- 2.5を使う計算
- 0.125を使う計算
- 分数と小数
- 100から引く計算
- 10、100、1000を作る計算
- 約数
- 倍数
- 素数
- 最大公約数
- 最小公倍数

小数・分数を含むカテゴリは、初期実装では4択を基本としてください。

初期バージョンでは、カテゴリ選択と基本出題が動作するところまで実装してください。

---

# 8. コインと報酬

## コイン獲得条件

- 問題に正解
- ミッション達成
- 自己ベスト更新
- 苦手問題克服
- 段を習得
- ボス撃破
- 連続学習

同じ簡単な問題を連打して無制限にコインを獲得できないよう、適切な上限または逓減処理を実装してください。

## コインの用途

- くくっちの服
- 帽子
- 部屋の壁紙
- 家具
- 背景
- エフェクト
- スタンプ
- ペット

ショップの商品はデータ定義から追加できるようにしてください。

---

# 9. 称号システム

称号は速度だけでなく、正確さ、努力、継続、成長も評価してください。

## スピード称号例

| 条件       | 称号       |
| -------- | -------- |
| 平均5秒以内   | かけざんビギナー |
| 平均4秒以内   | くくファイター  |
| 平均3秒以内   | くくマスター   |
| 平均2秒以内   | でんこうせっか  |
| 平均1.5秒以内 | 九九の神速    |
| 平均1秒以内   | くくレジェンド  |

称号判定には最低回答数を設定し、1問だけ速く答えた場合は付与しないでください。

## 正確さ称号

- 10問ノーミス
- 20問ノーミス
- 50問連続正解
- 100問連続正解
- 百発百中

## 継続称号

- 3日連続
- 7日連続
- 14日連続
- 30日連続

## 成長称号

- 初めて自己ベスト更新
- 前回より平均回答時間が1秒短縮
- 苦手問題を初めて克服
- すべての段を習得

## 段別称号

- 2の段マスター
- 3の段マスター
- 4の段マスター
- 5の段マスター
- 6の段マスター
- 7の段マスター
- 8の段マスター
- 9の段マスター
- 九九完全制覇

称号は条件定義をデータとして管理し、追加しやすくしてください。

---

# 10. デイリーミッション

毎日3つ表示してください。

例：

- 10問正解しよう
- 7の段を5問解こう
- 5コンボを達成しよう
- スピードモードを1回遊ぼう
- 苦手モンスターに3回勝とう
- 昨日の自分を超えよう

「昨日の自分を超えよう」は、前日のスピードモード自己ベストスコアを上回る、または前日より平均回答時間を短縮することを達成条件としてください。前日の記録がない場合は、このミッションを出題しないでください。

ミッション内容は、プレイヤーの習熟度に応じて変えてください。

日付判定は端末のローカル日付を使用してください。

日付変更時に新しいミッションを生成してください。

---

# 11. 習熟度判定

問題ごとに学習状態を保存してください。

以下を参考に型を設計してください。

```ts
type MultiplicationFactProgress = {
  id: string;
  left: number;
  right: number;
  correctCount: number;
  incorrectCount: number;
  consecutiveCorrect: number;
  averageResponseTimeMs: number;
  bestResponseTimeMs: number | null;
  lastAnsweredAt: string | null;
  nextReviewAt: string | null;
  masteryLevel: 0 | 1 | 2 | 3 | 4 | 5;
  recentResults: AnswerResult[];
};
```

習熟度：

- 0：未挑戦
- 1：練習中
- 2：少しできる
- 3：だいたいできる
- 4：定着
- 5：完全マスター

## 判定要素

- 正答率
- 連続正解数
- 回答速度
- 異なる日に正解しているか
- 最近の回答結果
- 最終回答日

単純な累計正解数だけで完全習得としないでください。

---

# 12. 復習スケジュール

簡易的な間隔反復を実装してください。

基本間隔：

- 翌日
- 3日後
- 7日後
- 14日後
- 30日後

正解が続けば間隔を伸ばし、不正解なら短くしてください。

復習期限を過ぎた問題は、通常問題やデイリーミッションへ適度に混ぜてください。

問題の出題優先度は、おおむね以下の順にしてください。

1. 最近間違えた問題
2. 復習期限を迎えた問題
3. 回答が遅い問題
4. 正答率が低い問題
5. 未挑戦問題
6. 習得済み問題

完全ランダム出題も別設定として残してください。

---

# 13. 回答方法

以下を実装してください。

- 4択
- 画面上の数字キーパッド
- PCの数字キー
- Enterキーで決定
- Backspaceで削除

## 数字キーパッド

- 0〜9
- けす
- こたえる
- 大きく押しやすい
- スマートフォンで画面下部へ固定可能
- 誤タップしにくい間隔

高学年モードの入力式問題では、小数点「.」キーを追加した小数用キーパッドを使用してください。低学年向け画面には小数点キーを表示しないでください。

問題によって、選択式または入力式を切り替えられるようにしてください。

---

# 14. 音と演出

音声ファイルがなくても動作するようにしてください。

初期実装ではWeb Audio APIによる簡単な効果音、または音なしでも成立する演出を使用してください。

## 音の実装上の注意

- iOS・iPadのSafariでは、AudioContextはユーザー操作後でないと音を再生できません。最初のタップ（タイトル画面や初回設定の決定ボタンなど）でAudioContextを生成・resumeする初期化処理を必ず実装してください。
- 読み上げはspeechSynthesisを使用し、日本語ボイスが取得できない場合はテキスト表示にフォールバックしてください。
- BGMは初期実装では実装しないでください。実装する場合もデフォルトOFFとします。効果音のみで演出が成立するようにしてください。

## 正解時

- 星
- コイン
- 小さなジャンプ
- コンボ表示
- 短い効果音

## 不正解時

- 画面を激しく揺らさない
- 大きな警告音を鳴らさない
- 正答をすぐ表示しすぎず、ヒントを出す
- 「おしい！」
- 「まとまりを見てみよう」
- 「もういちど！」

設定画面で以下を変更可能にしてください。

- 効果音
- BGM（実装した場合のみ）
- 読み上げ
- アニメーション軽減

---

# 15. データ保存

localStorageへ以下を保存してください。

- プレイヤー情報
- 設定
- コイン
- 経験値
- レベル
- 所持アイテム
- 装備中アイテム
- 問題別成績
- ゲーム履歴
- 称号
- ミッション
- モンスター図鑑
- 自己ベスト
- 最終プレイ日

## 保存設計

保存処理をコンポーネントへ直接書かず、以下のような層に分けてください。

```text
src/
  services/
  repositories/
  storage/
```

保存データにはバージョン番号を持たせてください。

```ts
type SaveData = {
  version: number;
  player: PlayerData;
  progress: ProgressData;
  settings: SettingsData;
};
```

将来の仕様変更に備えて、マイグレーション処理を用意してください。

## バックアップとひきつぎ（ユーザー向け正式機能）

iPad等のSafariでは、約7日間サイトを利用しないとlocalStorageが削除されることがあります。また、学校端末には年度末の初期化や入替があります。進捗消失を防ぐため、以下を開発用ではなく設定画面の正式機能として実装してください。

- 「データをほぞんする」：セーブデータをJSONファイルとしてダウンロードする。ファイル保存が難しい端末向けに、コピー用テキスト表示も用意する
- 「データをひきつぐ」：JSONファイルの読み込み、またはテキスト貼り付けで復元する
- 復元時にデータのバージョンが古い場合はマイグレーションを適用する
- 誤操作防止のため、復元・初期化の前に確認ダイアログを挟む
- 画面には保護者・先生向けの短い説明文を添える

## 開発用機能

開発用に以下も実装してください。

- セーブデータ初期化
- テスト用コイン追加
- 全ステージ解放
- JSONエクスポート・インポート（上記のユーザー向け機能と共通化して構いません）

開発用機能は、本番画面では目立たないようにしてください。

---

# 16. 推奨ディレクトリ構成

プロジェクトに適した形へ調整して構いませんが、おおむね以下の責務分離にしてください。

```text
src/
  app/
  components/
    common/
    game/
    character/
    rewards/
  pages/
  features/
    onboarding/
    home/
    learn/
    speed/
    random-games/
    monsters/
    boss/
    advanced/
    shop/
    titles/
    missions/
  game-engine/
    questions/
    scoring/
    mastery/
    review/
    rewards/
  hooks/
  services/
  repositories/
  storage/
  data/
  types/
  utils/
  styles/
  tests/
```

巨大な単一コンポーネントを作らないでください。

以下を分離してください。

- 問題生成
- 回答判定
- スコア計算
- 習熟度判定
- 復習優先度
- 報酬計算
- 称号判定
- ミッション生成
- セーブ処理
- UI表示

---

# 17. 問題生成

問題生成は再利用可能な純粋関数を中心に実装してください。

必要なカテゴリ：

```ts
type QuestionCategory =
  | "multiplication-basic"
  | "multiplication-square"
  | "pi-multiplication"
  | "two-digit-times-one-digit"
  | "two-digit-times-two-digit"
  | "divisors"
  | "multiples"
  | "prime"
  | "gcd"
  | "lcm";
```

7章の発展計算カテゴリを網羅できるよう、必要に応じてカテゴリを追加してください。

平方数の逆問題は、独立カテゴリにせず `multiplication-square` カテゴリ内で `metadata`（例：`direction: "forward" | "inverse"`）として表現してください。

問題の共通型を設計してください。

例：

```ts
type Question = {
  id: string;
  category: QuestionCategory;
  prompt: string;
  answer: number | string;
  choices?: Array<number | string>;
  explanation?: string;
  difficulty: number;
  metadata?: Record<string, unknown>;
};
```

4択の誤答候補は、正答と重複しないようにしてください。

誤答候補は、完全な乱数ではなく、子どもが間違えやすい近い値も含めてください。

例：

`7×8`の選択肢に、54、56、63、64などを適切に混ぜます。

## 九九の読み仮名データ

- 九九81件（1×1〜9×9）の読み仮名を `src/data/kukuReadings.ts` に定数データとして定義してください。
- 例：1×1「いんいちが いち」、2×3「にさんが ろく」、2×5「にご じゅう」、7×7「しちしち しじゅうく」、8×8「はっぱ ろくじゅうし」、9×9「くく はちじゅういち」
- 積が1桁のときは「が」が入り、2桁のときは入らない、といった慣用的な読み方に従ってください。
- テストで「81件すべて定義されていること」「ひらがな（と空白）のみで構成されていること」を検証してください。
- 読み仮名の正誤そのものは人間が最終確認します。完了報告でこのファイルのレビューを依頼してください。

---

# 18. 安全性と子どもへの配慮

以下を禁止してください。

- 外部広告
- 課金誘導
- チャット機能
- 子どもの実名公開
- 位置情報取得
- 不必要な個人情報収集
- マイクの使用（音声認識を含む）
- カメラの使用
- 無限スクロール
- 強い射幸性
- 有料ガチャ
- キャラクターが弱る演出
- 学習しないことへの罪悪感を与える表現
- 他児童との公開順位競争

宝箱の報酬は、必ずゲーム内で完結させてください。

---

# 19. アクセシビリティ

最低限、以下に対応してください。

- セマンティックHTML
- `aria-label`
- キーボード操作
- フォーカス表示
- 十分な文字サイズ
- 十分なコントラスト
- 色以外による状態表示
- `prefers-reduced-motion`
- 音なしでも理解できるフィードバック
- 読み上げを邪魔しないボタン名

---

# 20. テスト

以下の単体テストを必ず作成してください。

## 必須テスト対象

- 掛け算問題生成
- 選択肢の重複防止
- 回答判定
- スコア計算
- コンボ計算
- コイン計算
- 経験値計算
- 称号判定
- 習熟度判定
- 復習日の計算
- 苦手問題の抽出
- ミッション生成
- 九九の読み仮名データの件数・形式検証
- localStorageの保存・読込
- セーブデータのマイグレーション
- 日付変更時のデイリーミッション更新

## UIテスト

最低限、以下を確認してください。

- 初回設定からホームへ進める
- ゲームを開始できる
- 回答できる
- リザルトが表示される
- コインと経験値が加算される
- 再読み込み後も進捗が残る
- 音設定が保存される

---

# 21. README

README.mdに以下を記載してください。

- アプリ概要
- 主な機能
- 使用技術
- セットアップ方法
- 開発サーバー起動方法
- テスト方法
- ビルド方法
- GitHub Pages公開方法
- ディレクトリ構成
- セーブデータ仕様（バックアップ・ひきつぎ手順を含む）
- 今後の拡張案

「今後の拡張案」には、PWA対応（vite-plugin-pwaによるホーム画面追加・オフライン動作）と、Firebase/Supabaseによるクラウド保存を含めてください。これらは初期バージョンでは実装しません。

コマンド例：

```bash
npm install
npm run dev
npm run test
npm run lint
npm run build
npm run preview
```

---

# 22. 実装の優先順位

すべてを一度に雑に実装せず、以下の順で進めてください。

各Phase終了時に `npm run lint` / `npm run test` / `npm run build` を実行し、すべて成功した状態でgit commitしてください（コミットメッセージにPhase番号を含める）。

**今回のセッションではPhase 1〜2のみを実装し、Phase 2完了時点で停止して26章の形式で報告してください。Phase 3以降は次の指示で進めます。**

## Phase 1：基盤

- プロジェクト確認
- 型定義
- ルーティング（HashRouter）
- デザインシステム
- localStorage保存
- 問題生成
- 回答判定
- 基本レイアウト

## Phase 2：遊べるMVP

- 初回設定
- ホーム
- おぼえるモード
- スピードモード
- リザルト
- コイン
- 経験値
- レベル
- 称号
- 問題別成績
- 苦手問題の簡易一覧（正答率の低い問題をホームまたはリザルトから確認できる）
- バックアップ・ひきつぎ機能

## Phase 3：適応学習

- 習熟度判定
- 苦手問題抽出の本実装
- 復習スケジュール
- にがてモンスター
- 自動出題調整

## Phase 4：ゲーム性

- モンスターバトル
- 宝箱
- ロケット
- ショップ
- キャラクター育成
- デイリーミッション
- ボス戦

## Phase 5：高学年モード

- 平方数（□×□形式の逆問題を含む）
- 3.14計算
- 発展計算カテゴリ

## Phase 6：品質向上

- テスト
- アクセシビリティ
- レスポンシブ調整
- パフォーマンス改善
- README
- GitHub Pages対応の最終確認

---

# 23. MVPの完成条件

最低限、以下が実際に動作すればMVP（Phase 2）完成です。

- 初回設定ができる
- ホーム画面が表示される
- 段を選べる
- 4択または数字入力で回答できる
- 30秒スピードモードで遊べる
- 正解数と平均回答時間が表示される
- コインと経験値を獲得できる
- レベルアップできる
- 称号を獲得できる
- 問題別に正答率と回答時間が保存される
- 正答率の低い問題を一覧で確認できる
- データのバックアップ・ひきつぎができる
- 再読み込み後もデータが残る
- スマートフォンとタブレットで崩れない
- テストが通る
- 本番ビルドに成功する

---

# 24. 実装上の判断ルール

- 不明点があっても、合理的な仮定を置いて実装を進める
- 仮定した内容は最後に報告する
- 既存コードを壊さない
- 既存の設計思想がある場合は尊重する
- 依存パッケージを増やしすぎない
- 型安全性を優先する
- `any`を安易に使わない
- 警告を放置しない
- 未使用コードを残さない
- 仮ボタンだけの画面を大量に作らない
- 見た目だけでなくゲームとして操作可能にする
- TODOだけを残して完成扱いにしない
- エラー時は子どもにも分かりやすい表示にする
- 開発者向けエラーはconsoleへ記録する
- 乱数を使うロジックはテスト可能な設計にする
- 日付処理は関数へ分離する

---

# 25. 最終確認

各Phase完了時（特に今回のPhase 2完了時）に、以下を実行してください。

```bash
npm install
npm run lint
npm run test
npm run build
```

利用可能であれば、ブラウザまたはプレビュー環境で実際に操作確認してください。

確認項目：

1. 初回設定
2. ホーム表示
3. ゲーム開始
4. 正解時の処理
5. 不正解時の処理
6. 制限時間終了
7. リザルト表示
8. コイン加算
9. 経験値加算
10. 称号獲得
11. セーブ
12. 再読込
13. バックアップ・ひきつぎ
14. スマートフォン表示
15. タブレット表示
16. 高学年モード（実装済みの場合）
17. 音設定
18. アニメーション軽減

その時点で未実装のフェーズに属する項目はスキップし、報告に未実装である旨を明記してください。

問題が見つかった場合は、可能な限り修正してから完了してください。

---

# 25.1 Phase 7 追加仕様

## ボスコレクション

- ボスはデータ定義で管理し、1・2の段ボスから9の段ボス、全九九ボス、平方数ボス、円周率ボスを用意する。
- 低学年向けボスはボスバトル画面に配置し、高学年ボスはスーパー計算モード内に配置する。
- 段ボスは該当段で累計20問以上正解すると解放する。全九九ボスは段ボス8体クリアで解放する。
- 高学年ボスは該当カテゴリで累計20問以上正解すると解放する。
- 累計正解数で未解放のボスには、シルエットの下に `あと {n}もん で かいほう！` を表示する。残問数は必要正解数20から現在の累計正解数を引き、0以下にはしない。解放済みボスと全九九ボスには表示しない。
- 難易度はノーマル、ハード、さいそくの3段階とし、下位クリアで上位を解放する。
- ハードは1問6秒、さいそくは1問3秒の制限時間を設け、残り時間はバーと秒数の両方で表示する。
- 不正解や時間切れは即敗北にせず、ダメージなし、コンボ解除、「おしい」「もういちど」の文言で再挑戦できるようにする。
- 各ボスと難易度の初回クリア報酬は固定アイテム1個と固定称号1個にする。ランダムドロップ、ガチャ、射幸性のある演出は禁止する。
- 2回目以降のクリア報酬はコインのみとする。
- 図鑑にはボスとボス限定おたからの全スロットをNo.付きで表示し、未入手はシルエットと「？？？」で表示する。

## 九九の読み仮名

- 九九の読み仮名は `question` と `answer` に分割して保存する。
- 出題中は答え部分を「？」で伏せ、正解後または読み練習時に全文表示する。
- 読み仮名表示は式の上にルビ風に配置し、小さくしすぎない。
- 読み上げ設定がONの場合、正解後に全文を読み上げる。マイクは使用しない。

## 初回チュートリアル

- 初回設定後、最初にホームへ入ったタイミングで3〜5ページのチュートリアルを表示する。
- 各ページは短いメッセージ、絵文字または簡単な図、大きな「つぎへ」ボタン、「スキップ」ボタンを持つ。
- 表示済みフラグをセーブデータに保存し、2回目以降は自動表示しない。
- ホームまたは設定画面から「あそびかた」として再表示できる。

---

# 25.2 Phase 8 追加仕様

## げきムズ難易度

- ボス難易度に第4段階「げきムズ 🛸★★★★」を追加する。
- 各ボスの「さいそく ★★★」クリアで、そのボスの「げきムズ」を解放する。未解放時は4つ目のスロットを「？？？」として表示する。
- 段ボス・全九九ボスのげきムズは1問1.8秒、10問、HP10を基本値にする。
- 平方数ボスのげきムズは1問3.5秒、10問、HP10にする。
- 円周率ボスのげきムズは1問6秒、10問、HP10にする。
- げきムズはHP10/10問で、1問もミスできない上級者向けのノーミス挑戦とする。
- げきムズは4択固定とし、段ボス・全九九ボスでは約3割を虫食い形式（例：`□×7＝42`）にする。
- 高学年ボスの既存難易度は、平方数ボスをハード8秒・さいそく5秒、円周率ボスをハード12秒・さいそく8秒に緩和する。
- 不正解や時間切れは即敗北にせず、ダメージなし、コンボ解除、「おしい」「あとすこし」系の文言で再挑戦できるようにする。

## UFOコレクション

- 各ボスのげきムズ初回クリアで、そのボス固有のUFO1機と固定称号1個を付与する。ランダム報酬、ガチャ、ドロップ抽選は禁止する。
- 段ボスUFOは段の数だけライトを付ける。1・2の段ボスUFOは2ライト、3の段ボスUFOは3ライト、以降9ライトまで増やす。
- 全九九UFO、平方数UFO、円周率UFOはそれぞれ固有モチーフにする。
- 図鑑にUFOセクションを追加し、11機＋スペシャル1機の全12スロットをNo.付きで表示する。未入手はシルエットと「？？？」にする。
- UFOは1機だけ装備でき、装備中UFOはホーム画面でくくっちの上をふわふわ飛ばす。`prefers-reduced-motion` 時は静止表示にする。
- ボス詳細画面にはげきムズ初回クリア報酬のUFOプレビューを表示し、未入手時はシルエットにする。

## 全制覇報酬

- 11体すべてのげきムズをクリアした初回だけ、称号「すべてをしるもの」とスペシャルUFO「にじいろレジェンドごう」を付与する。
- 全制覇時は全画面のお祝い演出を表示する。`prefers-reduced-motion` 時は静的なお祝い表示にする。
- セーブデータはv3とし、v2以前のデータから `ownedUfos` と `equippedUfoId` を補完する。
- ランダム報酬は禁止し、UFO・称号・難易度パラメータは `src/data/` のデータ定義から追加・調整できる構造にする。

## ミニゲーム分離

- `/battle` はボス戦専用ルートとして維持する。
- ランダムゲーム側のミニゲーム「モンスターバトル」は `/monster-battle` として復活させ、モンスターバトル・宝箱・ロケットの3種を維持する。

---

# 25.3 Phase 9 追加仕様

## 問題難易度モデル

- 九九81問に `factDifficulty(a, b)` で★1〜★5の難易度を割り当てる。
- 重みは `2,5=1`、`3,4=2`、`6,9=3`、`7,8=4` とし、×1を含む問題は★1にする。
- raw重み合計は `2,3=>★2`、`4,5=>★3`、`6=>★4`、`7,8=>★5` に変換する。
- ★1は17問、★5は12問になることをテストで保証する。
- 難易度データは `src/data/` に置き、教師や運用者の判断で後から調整できるようにする。
- 6・7・8の段は誤答が増えやすい最難クラスタとして扱う。特に7・8を含む組合せは経験的にもつまずきやすいため、★5寄りに重み付けする。

## 経験値と出題フィルタ

- 正解時の経験値は `Question.difficulty` に比例させる。平均difficultyが約3になるため、係数は旧1問8XPに近い `8/3` を基準にする。
- 試行ボーナス、モードボーナス、コイン獲得の上限・逓減ルールは既存仕様を維持する。
- 問題生成は `minDifficulty` を受け取り、ボスはノーマル★1以上、ハード・さいそく★2以上、げきムズ★3以上、ミニゲームは★2以上を基本にする。
- minDifficultyで出題候補が空になる場合は、空にならないところまで自動で緩和する。

## スピードモードの段選択

- スピードモードは入室直後に開始せず、共通スタート画面を表示する。
- スタート画面には段選択チップ1〜9、ぜんぶ切替、30秒/60秒選択、スタートボタン、3・2・1カウントダウンを置く。
- 段選択は複数選択可、最低1段は必須とし、選択した段はセーブデータに保存して次回復元する。
- 選択した段の `×1〜×9` を出題対象に含める。
- 各段チップには平均difficultyの★を表示し、難しい段には「けいけんちアップ！」の動機付けを表示する。

## ミニゲーム差別化

- 3つのミニゲームにも共通スタート画面を置き、モード名、ひらがな中心の説明、スタートボタン、カウントダウン、もどるボタンを表示する。
- モンスターバトルはハート3つ、時間制限、攻撃予告、コンボでたまる手動ひっさつわざを持つ。
- ハート0でも責める敗北画面にせず、部分報酬を持って結果へ進める。
- 宝箱は時間制限なし、3問連続正解でカギ1本、終了後に3つの宝箱から自分で選ぶ。宝箱はハズレなしで、報酬差はコイン量のみとする。
- ロケットは正解で燃料と距離が伸び、速い正解ほど加速する。ミスで燃料が減り、燃料切れまたは規定問数で終了する。
- ロケット自己ベスト距離と到達バッジを保存し、図鑑に到達バッジ欄を表示する。

## セーブデータ

- SaveDataはv4とし、v3以前のデータから `speedSettings`、`rocketBestDistance`、`rocketBadges` を補完する。

---

# 25.4 Phase 9.1 追加調整

- Phase 9で追加したゲーム選択、スピード、ミニゲーム、ロケットバッジの文言はひらがな中心にする。
- ロケット到達バッジは上位を追加し、8段階にする。
- ロケットの到達距離は、上位バッジが十分な正解数と速い回答で届き得るように調整する。

---

# 25.5 Phase 10 追加仕様

## にがてモンスター判定

- にがてモンスターの登録は「その問題を間違えたとき」のみ発火する。
- 正解した問題は、回答が遅くても子どもに見えるにがてモンスターには登録しない。
- 平均回答時間は、復習出題の優先度と習熟度レベル4/5の内部判定には引き続き利用する。
- 克服条件は「合計3回正解、かつ1回は登録日と別の日に正解」とする。速度条件は使わない。
- SaveDataはv5とし、v4以前からの移行時に、間違い実績0件で時間だけにより登録されていた可能性があるにがて候補を削除する。
- ヘルプやチュートリアルの説明は「まちがえた もんだいが モンスターに なるよ」に寄せる。
- Phase 12.8では、にがてモンスターカードに克服までの進捗を表示する。残り正解数がある場合は `あと {n}かいで こくふく！`、別日条件だけ未達の場合は `また あした も といてみよう！`、両方残る場合は `あと {n}かい、また あしたも といてみよう！` とする。
- 克服済みモンスターには克服進捗を表示しない。
- Phase 12.8ではSaveDataをv9に上げ、v8以前からの移行時にも、間違い実績0件で時間だけにより登録されていた可能性があるにがて候補の削除を再適用する。JSONバックアップ読み込み時も同じマイグレーションを通す。

## ショップ拡張

- 通常ショップはボス限定おたからとは別枠で、くくっちの服・ぼうし・かぐ・かべがみ・はいけい・エフェクト・ペット系の20アイテムを持つ。
- 価格は1個目50コインから20個目10,000コインまで、おおむね昇順の固定データとする。
- 開店直後はNo.1〜10のみ表示する。
- 累計10個購入した瞬間にNo.11〜20が出現する。未購入のNo.11〜20は固定内容のシルエットと「？？？」、価格を表示し、購入後に正体を表示する。
- 1〜9個購入の間は、あと何個買うと新しいお店が開くかを表示する。
- ランダム販売、ガチャ的な抽選、ハズレ演出は実装しない。

## 1画面UI

- ゲーム中の画面は `100vh` フォールバック付きの `100dvh` と `overflow: hidden` を基準にし、問題表示から回答操作までスクロールなしで完結させる。
- 内部は上部ステータス、中部問題エリア、下部回答エリアの3ゾーンに圧縮する。
- 4択ボタンは2×2グリッドを基本とし、タップ領域44px以上を維持する。
- 問題式やボタン文字は `clamp()` で画面サイズに応じて縮小・拡大する。
- おぼえるモードのまとまり表示は高さに応じてドットを縮小し、テンキーは下部固定と `env(safe-area-inset-bottom)` でホームバーと重ならないようにする。
- 動作確認は360×640、390×844、768×1024、1024×768で、スピード・おぼえる・ボス・ミニゲーム3種・スーパー計算を対象にする。

---

# 25.6 Phase 11 追加仕様

## ホーム装備表示

- 通常ショップで入手したアイテムは、ホームに4つの装備スロットとして表示する。
- スロットは「ふく」「ぼうし」「へや」「なかま」とし、同じスロットのアイテムは1つだけ装備できる。
- ショップで装備ボタンを押した場合、他スロットの装備は維持し、同じスロットだけ差し替える。
- 装備していないスロットは空き表示にし、ホーム上で状態が分かるようにする。

## 称号カード

- ホームに所持称号のカードを表示し、押した称号を現在の称号として設定できる。
- 現在の称号カードは選択状態を、色だけでなくカードの状態として分かるようにする。
- 称号カードはホームの主要導線を圧迫しないよう、低めのカードとして配置する。

---

# 25.7 Phase 11.1 追加修正

## 初期設定の簡素化

- 初回設定画面から学習レベル選択を削除し、ニックネーム、アイコン、音のON/OFFだけにする。
- SaveDataの `player.learningLevel` は互換性のため残し、新規作成時は既定値 `first` を保存する。
- デイリーミッションなどで学習レベルを参照していた箇所は、全員共通の低学年標準設定に置き換える。
- 高学年モードは学習レベルによる制限をかけず、ゲーム選択やホームの導線から自由に入れる。

## ホームアイコン表示

- 初期設定で選んだアイコンを `player.icon` に保存し、ホームの名前周辺にプレイヤーアイコンとして表示する。
- プレイヤーアイコンはPhase 11の4装備スロットとは別扱いにする。
- 設定画面でアイコンを変更でき、ホーム表示に即時反映される。

## リザルト充実化

- 全モードのリザルトに「つぎのレベルまで あと◯EXP」のプログレスバーを表示する。
- 既存の `expToLevel()` と同じ式からレベル内進捗を算出し、レベルアップ時は「レベルアップ！」を表示する。
- スピードは正解数、問題数、正答率、平均回答時間、最大コンボ、スコアを確認できるようにする。
- おぼえるは、今回できるようになった問題数または習得問題一覧を表示する。
- ロケットは今回の到達距離、到達バッジ、次バッジまでの残り距離、自己ベスト更新を表示する。
- モンスターバトルは残りハート数、ひっさつわざ回数、獲得コインを表示する。
- 宝箱は獲得したカギ、開けた宝箱、内容、獲得コインを表示する。
- ボス戦は専用リザルトに勝敗、ボス残りHP、くくっち状態、難易度、初回報酬を表示する。
- スーパー計算はカテゴリ別の正答率を表示する。
- リザルトの操作ボタンには「もう一回」「あそぶへ」「ホームへ」を表示し、結果画面からゲーム選択へ直接戻れるようにする。

## 開始画面と学習カテゴリ

- 高学年モードは入室直後に問題を開始せず、開始画面で「ミックス」「平方数」「円周率」「発展」を選択してからスタートする。
- おぼえるモードは開始画面で「九九」「平方数」「円周率」を選択できる。九九は段と順番を選び、平方数・円周率は9問の4択練習として扱う。
- おぼえるモードの「けいさん」選択は、こたえかた（4択/入力）より左側に置き、円周率は小数回答の誤操作を避けるため4択固定にする。
- ホーム右側のくくっち/UFO窓は縦長に広げ、中央にくくっちを浮かべ、装備UFOや今後の装備表示を置ける余白を確保する。

---

# 25.8 Phase 12 追加仕様

## 図鑑ハブ

- 図鑑は1つの入口に統合し、タブは「くくっち」「モンスター」「UFO」「おたから」「コレクション」とする。
- 各タブはNo.付きスロットで表示し、未入手はシルエットと「？？？」で表示する。
- 各タブ上部に「あつめた かず ◯／◯こ（◯%）」を表示する。
- 図鑑トップに全体メーター「ぜんぶで ◯／◯こ あつめた！」を表示する。
- スロットをタップすると、名前、説明、入手日、入手方法をモーダル表示する。
- 「おたから」タブにはボス討伐記録とボス限定アイテムを集約する。
- 「コレクション」タブには宝箱アイテム20種と鍵5種を集約する。

## 宝箱アイテム

- 宝箱アイテムは `src/data/treasureItems.ts` に20種定義する。
- テーマは、ほし系、うちゅう系、きらきら系、いきもの系、おかし系の5分類とする。
- レアリティは★1ノーマル、★2レア、★3スーパーレア、★4ウルトラレアで表す。
- 各テーマは★1〜★4を1個ずつ持ち、全体で5テーマ×4レア=20種、各レア5種にそろえる。
- ほし系は、ちびほしチップ★1、ほしのベル★2、あまのがわほし★3、ほしのおうかん★4。
- うちゅう系は、ろけっとねじ★1、すいせいびん★2、リングわくせい★3、ぎんがちず★4。
- 宝箱の中身決定は純粋関数にし、乱数はシード可能にする。
- 宝箱は色と説明でレアリティ帯を事前に示す。ハズレは作らない。
- 宝箱を開けるときは、その宝箱のレア範囲内の未所持アイテムを優先して抽選する。
- 未所持候補が残っている間は同じアイテムを出さず、候補が空のときだけ箱別の固定コインへ変換する。
- 全20種を集めた後は、すべての宝箱が固定コイン報酬になる。図鑑のコレクション入手率は100%表示になる。

## 鍵と宝箱のデザイン体系

- 鍵は `src/data/keys.ts` に5種定義する。どうのカギ、きんのカギ、だいやのカギ、にじいろカギ、ほしのカギ。
- 各鍵は対応する宝箱を1種類開けられる。
- 宝箱は、どう、きん、だいや、にじ、ほしの5種類とし、それぞれ★帯と枯渇時コインをデータで持つ。
- レア範囲は階段状にする。どう=★1、きん=★1〜2、だいや=★2〜3、にじ=★3〜4、ほし=★4。
- 枯渇時コインは、どう20、きん40、だいや70、にじ110、ほし160を初期値とする。
- 鍵と宝箱アイテムは専用SVGコンポーネントで描画する。
- SVGはベース形、色、装飾レイヤーを合成する。レアリティが高いほど装飾点や枠が増える。
- レアリティは色だけでなく★数でも表示する。

## セーブデータ

- SaveDataはv6とし、v5以前から `ownedTreasureItems` と `treasureKeys` を補完する。
- 宝箱アイテムは入手ID、入手日、入手方法を保存する。
- 鍵は種類ごとの入手数と初回入手日を保存する。

# 25.9 Phase 12.2 追加調整

## ロケットバッジ

- ロケット最終バッジ「こすもすれじぇんど」の到達距離は920mとする。
- 直前の「ぎんがすらいだー」840mから60m差とし、最終到達を少し現実的にする。

## 入手日の記録

- 図鑑で入手済みに見えるものは、取得記録があれば入手日を表示する。
- 既存データなどで取得記録がない入手済みコレクションは「きろくなし」と表示し、未入手は「まだです」と表示する。
- SaveDataはv7とし、共通の `collectionRecords` を補完する。
- 今後入手するロケットバッジ、にがてモンスター、ボス限定アイテム、UFOは `collectionRecords` に入手ID、入手日、入手方法を保存する。
- 宝箱アイテムと鍵は、既存の `ownedTreasureItems` と `treasureKeys.firstAcquiredAt` を引き続き正とする。
- JSONバックアップはSaveData全体を出力するため、v7の `collectionRecords` も含める。

## ホームとショップ

- ホームの「あいさつ行」は表示しない。
- ホームの「あそびかた」は名前まわりから上部バー右端へ移動する。
- ホームの今日のミッションは、名前と称号の右側に配置する。小さい画面では縦並びに戻す。
- ショップの「かう」は青、「そうび」は黄色、「そうびちゅう」はオレンジで表示する。
- ショップ上部に「ぜんぶ」「ぼうし」「ふく」「はいけい」「かべがみ」「かぐ」「ひかり」「なかま」のカテゴリ絞り込みを置く。

# 25.10 Phase 12.3 追加調整

## ドット絵モンスター

- モンスターの正体、所持判定、No.、ゲーム挙動、SaveDataは変更しない。SaveDataはv7のまま据え置く。
- モンスターは九九の式 `left x right` から決定的にSVG生成する。同じ式は常に同じ絵になる。
- モンスター図鑑は `1x1` から `9x9` までの81枠とし、`2x3` と `3x2` は別スロットとして扱う。
- 色は段 `left` で決まる。共有パレットは、1=あか、2=むらさき、3=あお、4=みどり、5=きいろ、6=だいだい、7=もも、8=みずいろ、9=きみどり。
- 形はかける数 `right` で決まる。小さなピクセル配列をベース形として持ち、式シードから目、口、ハイライト、模様を少し変える。
- 輪郭、影、ハイライトは段の基準色から自動算出する。
- 図鑑グリッドとにがて復習の開始画面では、絵文字ではなく `<MonsterSprite />` を使う。

## ドット絵トロフィー・メダル

- ボス限定おたからのキーは、既存どおり `bossId + difficulty` とする。保存IDや報酬ロジックは変えない。
- 難易度は `normal`、`hard`、`fast` の3区分をボス限定おたからとして扱い、`gekimuzu` は既存どおりUFO報酬とする。
- `normal` と `hard` はメダル型、`fast` はトロフィー型で表示する。上位表示用に `gekimuzu` の色設定も保持する。
- メダル面またはトロフィー板には段ラベルを表示する。例: `1・2`、`3`、`全`、`平`、`3.14`。
- 難易度で金属色を変え、段パレットをリボンや縁のアクセントに使う。
- 図鑑のおたからタブでは、ボス討伐カードとボス限定アイテムを絵文字ではなく `<TrophySprite />` で表示する。
- スプライト生成は純粋関数に分離し、決定的生成をテストで保証する。

# 25.11 Phase 12.4 追加調整

## 高学年なかま

- 高学年なかまは、平方数、3.14、ミックスの3カテゴリに分ける。
- 既存SaveDataの `categoryCorrect` から入手判定し、今後入手したものは `collectionRecords` に `advanced-monster:<id>` として入手日と入手方法を記録する。
- SaveDataはv7のまま据え置く。新フィールドとマイグレーションは追加しない。
- 図鑑の「モンスター」タブに「こうがくねんの なかま」セクションを追加する。未入手はシルエットと「？？？」で表示する。
- 平方数なかまは2×2から9×9までのブロック体で描く。7×7以上は数字ラベルも添えて読みやすさを保つ。
- 3.14なかまは、円形の体と1〜2本のリングで描く。青紫〜シアンの専用色を使う。
- ミックスなかまは、段パレットを複数混ぜたパッチワークで描く。
- 高学年なかまは20枠とする。平方数8枠、3.14が6枠、ミックスが6枠。

## 高学年ボスのキャラデザイン

- 平方数ボスと円周率ボスは、Phase 8のバランス、HP、秒数、報酬、解放条件を変更しない。
- 平方数ボスは、スクエアダイヤごうと揃えた四角・結晶ゴーレムのピクセルスプライトにする。
- 円周率ボスは、パイくるりんごうと揃えたリング惑星のピクセルスプライトにする。
- ミックスは現在プレイ可能ボスを持たないため、全ボス数や全制覇報酬に影響させない。高学年カテゴリ用のにじいろ巨大ボス風スプライトだけを用意する。
- 高学年モードのカテゴリ表示、平方数ボス・円周率ボスのカード、バトルHUDでは絵文字プレースホルダを使わない。
- スプライトは静止SVGで、同じ対象は常に同じ絵になる決定的生成とする。

# 25.12 Phase 12.5 追加調整

## 高学年コンテンツの命名

- 円周率なかまのIDと獲得条件は変更せず、表示名だけをリング惑星テーマに揃える。
- `pi-1` は「ワンリングパイ」、`pi-2` は「ツインリングパイ」、`pi-5` は「ファイブリングパイ」、`pi-10` は「スリーリングパイ」、`pi-25` は「フォーリングパイ」、`pi-100` は「フルムーンパイ」とする。
- `pi-5` は5つながりでファイブを維持し、`pi-25` はクォーターの4分割イメージをフォーに寄せる。IDはすべて据え置く。
- 平方数ボスの表示名は「クリスタルゴーレム」、円周率ボスの表示名は「リングプラネット」とする。ミックスの特別スプライト名は「にじいろキング」とする。
- ボスID、報酬ID、バトルバランス、スプライト形状は変更しない。SaveDataはv7のまま据え置く。

## 図鑑の見出し

- 図鑑の「モンスター」タブは、九九81枠を「ていがくねんの なかま」、高学年20枠を「こうがくねんの なかま」に分けて表示する。
- ナンバリング、並び、所持判定、入手記録は既存仕様を維持する。

# 25.13 Phase 12.6 追加調整

## 学習・バトル画面

- おぼえるモードのゲーム中画面は、上部の段選択とキャラクター枠を圧縮し、回答選択肢が1画面内に見えるようにする。
- おぼえるモードは開始時に段選択画面を表示する。1〜9の段から1つ選び、順番は「ランダム」「上がり 1→9」「下がり 9→1」から選べる。
- おぼえるモードは選んだ段の9問をすべて出題する。スタートボタンのカウントダウン後にゲーム画面へ入る。
- ボスバトルは難易度を選んだあと、共通のスタート画面とカウントダウンを表示してから開始する。
- モンスターバトルは開始画面で使用する段を複数選択でき、選んだ段の問題と対応するモンスターだけが出る。
- ボス報酬UFOのライトは丸の横並びではなく、色付きの星ライトを小さく折り返して表示する。
- スピードモードのゲーム中ステータスは、のこり時間、スコア、れんぞくを横並びにし、宇宙船キャラクターを小さく表示して1画面内に収める。

## ホームUFOと設定

- SaveDataはv8とし、`player.shipName` を追加する。既存データは `くくっち` を既定名として補完する。
- 設定画面で宇宙船名を変更できる。名前はかな5文字まで、空文字、かな以外、指定した不適切語を含む文字列は保存しない。
- ホーム画面のUFO窓は `player.shipName + "号"` を表示する。
- UFO窓の「クルー待機中」相当の文字は、所持済み称号から選んだ `player.currentTitle` を表示する。設定画面とホームの称号カードで変更できる。

# 25.14 Phase 14 追加仕様

## がっこうモード: 1日のじかんバジェット

- 1日の実プレイ時間を端末ローカルで数え、既定は10分とする。
- 先生・保護者向け設定で、先生コード入力後にだけバジェットを `オフ`、`10分`、`15分`、`20分` から選べる。子どもが設定画面を開いただけでは分数ボタンを表示しない。ハードロックは作らない。
- 実プレイ時間は、アプリが表示中かつフォーカス中で、約55秒以内に操作があったときだけ加算する。バックグラウンド、blur、放置中は加算しない。
- 使用時間はセッションをまたいで合算し、端末ローカル日付が変わったら0に戻す。端末時計が過去へ戻っても使用時間が壊れないよう、未来日付の記録は勝手にリセットしない。
- バジェットに達したら、その日の残りはコインとEXPの付与だけを止める。遊び、ボス、げきムズ、高学年、ミックス、図鑑閲覧は止めない。
- トロフィー、メダル、UFO、称号、図鑑登録、ロケットバッジ、ボス初クリア、難易度解放など、1回きりの達成記録はバジェット後も通常どおり保存する。
- バジェット到達後は、今遊んでいるモードやゲームが終わった結果画面で初回だけテロップウインドウを表示し、`10ぷん たったよ`、`このあとは コインとけいけんちは たまらないよ`、`あそびと きろくは そのまま つづけられるよ` と伝える。10分以外に設定されている場合も、`ぷん` 表記で表示する。
- 報酬オフ中は上部バーや結果画面に `きょうの ごほうびは おしまい` と表示し、罰やロックの文言は使わない。
- 1日の使用時間と通知済み日付は、SaveDataとは別のlocalStorageキー `kukucchi_dailyUsage` に保存する。JSONバックアップには含めない。
- バジェット分数設定は既存の `settings.dailyBudgetMinutes` に保存する。SaveData versionはv9のまま据え置く。

# 25.15 Phase 14.1 追加調整

## 共通フッター

- すべての `AppShell` 画面末尾に共通フッターを表示する。
- 文言は「「あったらいいのに」を、作ってる。」と「© 2026 野村晃一 All rights reserved.」とする。
- ゲーム中画面では操作を邪魔しないよう、固定表示にはせず、末尾に小さく表示する。

## 図鑑の称号タブ

- 図鑑に「しょうごう」タブを追加する。
- 称号は既存の称号データ、ボス報酬称号、全制覇称号から定義を作る。
- 取得済み称号は名前、説明、入手日、入手方法を表示する。未取得称号はシルエットと「？？？」で表示し、説明を伏せる。
- 取得済みを先、未取得を後ろに並べる。取得済みは入手日の新しい順にする。
- 今後取得した称号は `collectionRecords` に `title:<称号名>` として入手日と入手方法を保存する。既存の称号で入手日がないものは「きろくなし」と表示する。

## ホームUI

- ホーム画面のUFO/くくっち窓は、360×640、390×844、768×1024、1024×768で見切れないように、キャラクターと装備UFOを一回り小さく表示する。
- レベル、EXP、コインの数値は子どもが見つけやすいよう、ホームの小カードでは `clamp(1.42rem, 4.8vw, 1.72rem)` を基準に拡大する。

## 高学年ボス3区分

- 高学年ボスは「へいほうすう」「えんしゅうりつ」「はってん」の3区分に分ける。
- へいほうすうは平方数ボス、えんしゅうりつは円周率ボス、はってんはミックス/発展問題のにじいろキングを扱う。
- 3区分は解放判定、バトル、クリア判定、報酬が独立して動く。はってんボスの解放は `categoryCorrect.development` を参照する。
- SaveDataはv10とし、v9以前からの移行時に旧高学年ボス（平方数ボス、円周率ボス）のクリア記録、旧ボス限定アイテム、旧UFO、旧称号、対応する取得記録をリセットする。新しい3区分のクリア状況は公開後に取り直す。
- JSONバックアップの読み込み時も同じマイグレーションを通す。

## 流れ星

- 流れ星は右上から左下へ流れる。速度、量、透明度は既存のまま、移動方向だけを変更する。

# 25.16 Phase 14.2 追加仕様

## がっこうモード2: 習熟報酬テーパリング

- `settings.schoolMode2Enabled` を追加し、既定ONとする。先生・保護者用設定でON/OFFできる。SaveDataはv11とする。
- 対象は練習系の `learn` / `review` に限定する。ボス戦、スピード、ミニゲーム、高学年の固定構成は報酬倍率と問題選択を変えない。
- 式ごとの既存 `MultiplicationFactProgress` から習熟帯を算出する。新しい式、未習熟、にがて未克服は `みならい`。`masteryLevel >= 2` など中程度は `なれてきた`。`masteryLevel >= 4` は `とくい` とする。
- 報酬倍率は `みならい=100%`、`なれてきた=70%`、`とくい=20%`。マスター済みでも0にはせず、元の報酬がある場合は最低1コイン/1EXPを残す。
- 結果画面では倍率が100%未満のとき、「マスター！すごい！」として前向きに表示する。罰やロックの文言は使わない。
- 図鑑やにがて練習の式表示では、`とくい` の式に `★ とくい` マークを表示する。

## がっこうモード2: 適応難易度

- `review` の次問選択は、学校モード2がONのとき未マスター・にがて・最近ミスした式を重めにする。
- マスター済みの式は出現重みを大きく下げるが、完全には除外しない。
- 連続で2問以上間違えた場合は、低難度または既知の式を優先し、正答率が極端に落ちないようにする。
- 学校モード2をOFFにすると、従来のレビューキュー優先・通常ランダム選択に戻る。
- 習熟度は既存の正解数、連続正解数、平均回答時間、別日回答を使って算出するため、新しい学習進捗フィールドは追加しない。

## SaveData v11

- v10以前のデータは `settings.schoolMode2Enabled: true` を補完する。
- Phase 14.1 の旧高学年ボスリセットは v10 未満のデータにのみ適用する。v10からv11へ上がるだけのデータでは再リセットしない。

# 25.17 Phase 15-1 追加仕様

## くくっち号カスタム: プレビュー窓の土台

- ホーム右側のUFO窓を、装備が常に見える「くくっち号プレビュー窓」として扱う。
- 旧Phase 13で検討していた6x4グリッドの部屋は作らない。コインで買う見た目の反映先は、このUFO窓カスタムへ一本化する。
- プレビュー窓は背面から前面へ、`はいけい`、`UFO船体`、`くくっち本体`、`ぼうし`、`なかま枠`、`エフェクト枠` の6レイヤーで重ねる。
- Phase 15-1では既存の所持・装備データだけを使い、`はいけい`、`UFO`、`ぼうし` の3レイヤーをホームで切り替えられるようにする。
- くくっち本体は現状の見た目で固定する。スーツ差し替え、なかまの同乗、エフェクトの中身、克服連動のなかま入手はPhase 15-2で扱う。
- `はいけい` はショップの `wallpaper` / `background` のうち `visual.layer = window` のものを使う。
- `UFO` は既存の `progress.equippedUfoId` と `ownedUfos` を使い、ボス報酬UFOの見た目を窓内の船体へ反映する。
- `ぼうし` はショップの `hat` 装備を使い、くくっちのヘルメット上の小物として反映する。
- ホームのカスタムUIで選んだ装備は、その場でプレビュー窓に反映する。保存は既存の `progress.equippedItems` と `progress.equippedUfoId` を使うため、SaveDataバージョンは変更しない。
- ⑤なかま枠と⑥エフェクト枠は空のスロットとしてz順だけ確保し、15-2で中身を差し込める構造にする。
- `vite.config.ts` の `base` は `/math-planet/` とする。

## キャラ名・ロケット・レベルアップ演出

- 設定画面で、船名とは別にくくっち本体の名前を変更できる。保存先は `player.characterName` とする。
- キャラ名は船名と同じ `validateShipName` を使い、空文字、5文字超、禁止語を拒否する。カタカナ正規化と濁点除去の方針も船名と同じにする。
- 既存セーブは `player.characterName` が無い場合、既定名 `くくっち` で補完する。SaveDataはv12とする。
- ホームのくくっち号窓では、キャラ名をくくっち本体の表示名として出し、アクセシビリティラベルにも反映する。
- ロケット最終バッジ「こすもすれじぇんど」の到達距離は、Phase 15-1以降は920mとする。
- 結果画面のEXPバーは、前回EXPから今回EXPまでアニメーションで伸びる。レベル境界をまたぐ場合は100%まで伸び、次レベルの0%から続けて表示する。
- レベルアップ時は「レベルアップ！」のネオン演出を表示する。複数レベル上昇でもステップを分けて連続表示し、スキップボタンで演出を送れる。
- `prefers-reduced-motion` ではEXPバーのトランジションとレベルアップ演出の動きを抑える。

# 25.18 Phase 15-2 追加仕様

## くくっち号カスタム: スーツ

- プレビュー窓の③くくっち本体に、スーツ色と胸マークを重ねて表示する。
- スーツは既存のくくっち本体SVG/CSSのカラーレイヤー差し替えで表現し、ベース形は変えない。
- 初期スーツは `ネイビースーツ`、`ホワイトスーツ`、`レッドスーツ`、`グリーンスーツ`、`パープルスーツ` の5種とする。
- スーツはショップで購入できる。価格は200、250、300、350、400コインとし、くくっち号カスタムで気軽に買える価格帯にする。
- ホームのカスタムUIに `スーツ` を追加し、所有済みスーツを選ぶとプレビュー窓に即反映する。

## くくっち号カスタム: なかま

- プレビュー窓の⑤なかまレイヤーに、選択中のなかま1体だけを同乗表示する。
- なかまはくくっちの邪魔にならない右下寄りに、くくっち本体の約1/3から1/4程度の大きさで表示する。
- にがてモンスターを克服した場合、そのモンスターのドット絵をなかまとして選択できる。購入は不要で、克服記録を入手条件とする。
- 専用なかまは12種とする。ショップ入手は `ほしクラゲ`、`うちゅうネコ`、`スペースうさぎ`、`コスモペンギン`、`ちびほし`、`みかづきん`、`わくせいくん`、`ナビロボ`、`ミニドロイド`、`スターボット`、`コスモナビ` の11種。宝箱入手は `にじほし` の1種。
- 専用なかまのドット絵は、IDから決定的に生成するSVGピクセルスプライトとする。外部画像は使わない。
- `にじほし` はにじのたからばこ、またはほしのたからばこから入手できる。未所持の場合だけ宝箱候補に入り、入手後は通常の枯渇コイン処理に戻る。
- ⑥エフェクトレイヤーはPhase 15-2では枠のままとし、中身はPhase 15-3で扱う。

## 図鑑とショップ

- 図鑑に `なかま` タブを追加する。
- `なかま` タブは、克服モンスター81枠の `にがてをこくふく！` ブロックと、専用なかま12枠の `ショップとたからばこ` ブロックに分ける。
- 未入手のなかまは既存図鑑と同じくシルエットと `？？？` で表示する。取得済みは名前、説明、入手日、入手方法を表示する。
- ショップのカテゴリ絞り込みに `スーツ` と `なかま` を追加する。ショップなかま購入時は `collectionRecords` に `buddy:<id>` として入手日を記録する。

## SaveData v13

- `progress.equippedBuddyId` を追加する。値は `monster:2x3` のような克服モンスター選択ID、または `buddy:star-jelly` のような専用なかま選択ID、未選択時は `null` とする。
- v12以前のデータは `equippedBuddyId: null` を補完する。
- スーツの装備状態は既存の `progress.equippedItems` を使うため、新しいスーツ専用フィールドは追加しない。
- 専用なかまの所持は `collectionRecords` に統合し、JSONバックアップにも含める。

# 25.19 Phase 15.2 UIブラッシュアップパッチ

## UI修正

- おぼえるモード、スピードモード、ボス開始画面などの開始前UIは、説明文・段選択・スタートボタンが重ならないよう `mode-ready-shell` で3ゾーン構成に整理する。
- ゲーム中の選択肢ボタンは `display:flex; align-items:center; justify-content:center` を使い、数字や文字が常に縦横中央に来るようにする。
- スピードモード等のくくっち表示は、ゲーム中のコマンド領域に収まるよう小さめの専用サイズを使う。
- ロケットなどの開始画面の「もどる」ボタンは画面幅内に収まるよう、スタートボタンと横並びの可変幅にする。
- ボス難易度選択後の開始画面では「もどる」でボス選択へ戻れるようにする。

## カギ入手経路

- 宝箱チャレンジでは3問連続正解ごとに `どう -> きん -> だいや -> にじいろ -> ほし` の順でカギを入手する。
- にじいろのカギ、ほしのカギは宝箱チャレンジだけでも入手可能だが、上位の達成報酬としてボスげきムズ初回クリアにも固定付与する。
- 段ボスのげきムズ初回クリアは `にじいろのカギ`、全九九ボスおよび高学年ボスのげきムズ初回クリアは `ほしのカギ` を1本付与する。
- 2回目以降のクリアでは同じカギを再付与しない。SaveDataの `progress.treasureKeys` に既存形式で加算する。

## 宝箱リザルト

- 宝箱リザルトにはテキストだけでなく、入手したカギ・宝箱アイテム・宝箱なかまのSVGアイコンを表示する。
- カギは `KeyIcon`、アイテムは `TreasureIcon`、なかまは `BuddySprite` を使う。

## 名前変更クールダウン

- キャラ名と船名の変更は、保存ボタンで確定する。
- 名前変更後、同じ対象を次に変更できるのは168時間後とする。
- クールダウン中は「つぎに かえられるのは ◯がつ◯にち◯じ です」と表示する。
- クールダウン日時は SaveData ではなく別localStorageキー `kukucchi_nameChangeCooldown` に保存する。JSONバックアップには含めない。
- 入力バリデーションは既存の `validateShipName` を共通利用し、かな5文字まで・禁止語拒否を維持する。

## 円周率モード

- 円周率モードの `3.14 × n` は、かける数 `n` を1〜9の1桁に限定する。
- 2桁以上の `3.14 × 18` などは出題しない。

## SaveData

- 本パッチではSaveDataバージョンを変更しない。カギは既存の `progress.treasureKeys`、名前変更クールダウンは別localStorageキーを使う。

# 25.20 Phase 15.3 カスタム統合・UI磨き

## もちもの画面

- ホームの重複していた `スピード` 大カードは `カスタム` に置き換える。スピードモード自体は `あそぶ` 内から引き続き利用できる。
- カスタム画面は `プレビュー窓 + タブ式もちもの` とする。タブは `はいけい`、`UFO`、`ぼうし`、`スーツ`、`なかま`、`エフェクト`、`しょうごう` の7つ。
- 各タブはショップ購入、ボス報酬、宝箱、克服モンスター、称号など、入手元を問わず同じグリッドに集約する。
- 未所持の枠は `？？？` とシルエットで表示し、タブ上部に `所持数/総数` を表示する。
- 所持済みの項目をタップするとその場で装備し、UFOプレビュー窓へ即反映する。保存は既存の `progress.equippedItems`、`progress.equippedUfoId`、`progress.equippedBuddyId`、`player.currentTitle` を使う。
- ショップは `買う場所`、カスタムは `着せる場所` として役割を分ける。ショップ内のカスタムを示すサブ文言は整理する。

## 称号とバッヂ

- 称号はホームの名前下に表示する肩書き専用とする。
- UFO窓内の肩書きテキストは表示しない。窓内には装備中称号を示す小さなバッヂアイコンを1つだけ表示する。
- 称号の選択はカスタム画面の `しょうごう` タブで行う。

## レベルアイコン

- Lv5、10、15、20、25、30、35、40で宇宙テーマの名前横アイコンを解放する。
- 解放アイコンはレベルから導出し、既存プレイヤーは現在レベルまでに解放されるアイコンを自動的に使える。
- 設定画面およびカスタム画面で、解放済みアイコンを選択できる。
- 結果画面でレベルアップにより新アイコンが解放された場合、EXPバー演出の後に `あたらしい アイコンが ふえたよ！` とアイコン名を表示する。表示は閉じられる。

## なかまの見た目

- 専用なかま12種は、IDごとに耳、リング、アンテナ、ひれなどのピクセル特徴を加え、同じテーマ内でも見分けられるようにする。
- 描画方式はPhase 12.3/15-2と同じ、決定的なSVGピクセルスプライトとする。

## UI磨き

- 操作可能なボタン・リンクはポインターカーソルにする。
- 開始画面の `もどる` ボタンは、スタートボタンと同じ横幅基準の2カラムにし、小さい画面でも右端が切れないようにする。
- ショップカードは高さを揃え、買う/装備ボタンをカード下端に揃える。
- にがて欄には、実際の克服条件に合わせて `べつの日に また せいかいすると きえるよ` と薄く表示する。`3回連続` など条件と異なる表現は使わない。

## SaveData

- 本フェーズではSaveDataバージョンを変更しない。レベルアイコンは `player.level` から解放状態を導出し、カスタム選択は既存フィールドへ保存する。

# 25.21 Phase 15-3 エフェクト実装・デバッグメニュー

## くくっち号カスタム: エフェクト

- プレビュー窓の⑥エフェクトレイヤーに、見た目だけの演出を重ねる。ゲーム効果や報酬補正は持たせない。
- エフェクトは6種とする。ショップ入手は `ほしのこな`（300コイン）、`ながれぼし`（350コイン）、`オーラリング`（400コイン）、`ふわふわひかり`（350コイン）。
- 宝箱限定は `にじオーラ` とし、にじいろのたからばこ以上の宝箱から未所持優先で入手できる。
- 全ボスをげきムズで1回ずつ撃破したとき、全制覇報酬として `ぎんがのうず` を自動付与する。既にスペシャルUFOを持つセーブでも、`ぎんがのうず` が未所持なら次の全げきムズ判定で補完できる。
- カスタム画面の `エフェクト` タブには、ショップ・宝箱・ボス報酬の入手元を問わず全エフェクトを表示する。未所持はシルエットと `？？？` 表示にする。
- エフェクトはCSS/SVGレイヤーで描画する。`ながれぼし` は右上から左下へ流れる。外部ライブラリは使わない。
- `requestAnimationFrame` で実フレームレートを軽く計測し、30fpsを下回る場合は `low` 表示に切り替えて粒子数や動きを減らす。`prefers-reduced-motion` の端末では `static` 表示にし、移動アニメーションを止める。

## 開発者デバッグメニュー

- 設定画面のバージョン表示を5回連続タップすると、隠しメニューとして `かいはつしゃメニュー` を表示する。本番ビルドでも残すが、通常の設定一覧には見せない。
- デバッグメニューには `フルオープン`、`100/500/1000コイン追加`、`レベル設定`、`カギ全種追加`、`SaveDataリセット`、`現在のSaveData表示` を置く。
- `フルオープン` はショップアイテム、UFO、なかま、称号、エフェクト、カギ、宝箱コレクション、九九モンスター図鑑をテスト確認用に解放する。
- `SaveDataリセット` は `けしますか？` と `ほんとうに けしますか？` の2段階確認を必須にする。
- デバッグメニューを開いたこと自体のlocalStorageフラグは保存しない。デバッグ操作で付与した所持品は通常のSaveDataへ書き込む。

## SaveData

- 本フェーズではSaveDataバージョンを変更しない。エフェクト所持・装備は既存の `progress.ownedItems` と `progress.equippedItems`、入手日は既存の `collectionRecords` に保存する。

# 25.22 Phase 15.4 UIバグ修正パッチ

## おぼえる開始画面

- おぼえるモードなどの開始前画面は、前画面が残って見えないよう `mode-ready-shell` のルートと開始パネルに不透明な宇宙背景を敷く。
- `mode-ready-shell` はゲーム中と同じ小型フッター扱いにして、開始前 UI の有効高さを確保する。

## くくっち顔表示

- 開始前画面のくくっちは、部品を固定pxで潰さず、UFO・顔・目・アンテナ・口の比率を保って縮小表示する。
- おぼえるモードの開始画面でも顔、アンテナ、UFO船体がクリップされないサイズを使う。

## もどるボタン

- `mode-start-actions` はスタートと戻るを同じ幅基準の2カラムにし、`max-width: 100%`、`box-sizing: border-box`、`overflow: hidden`、`min-width: 0` を適用する。
- ミニゲーム開始画面にも `mode-ready-shell` を付け、ロケット・宝箱・もんすたーばとるの戻るボタンにも同じ幅制御を効かせる。

## もんすたーばとる表示

- もんすたーばとるのゲーム中ヘッダーは、モンスター表示、説明、HP/数値メーターを独立したブロックとして並べる。
- 小型画面用メディアクエリでもバトルヘッダーを過度に圧縮せず、モンスターのドット絵とメーターが重ならない高さを確保する。
- 本フェーズでは SaveData とゲームバランスは変更しない。

# 25.23 Phase 15.5 カスタム拡大・称号エンブレム・最終称号

## カスタム画面

- カスタム画面のUFOプレビュー窓は、装備したUFO・くくっち・ぼうし・スーツ・なかま・エフェクトが見やすいように大きく表示する。
- 所持品グリッドのサムネイルも大きめにし、スマホ縦幅でも押しやすいカードサイズを保つ。
- 360×640、390×844、768×1024、1024×768でプレビューとグリッドが見切れないレスポンシブ寸法にする。

## 称号エンブレム

- 称号バッヂは単一の黄色丸ではなく、SVGのエンブレムとして描く。
- エンブレムは、枠・地色・モチーフ・装飾を組み合わせた決定的生成とし、画像ファイルは増やさない。
- レアリティは `common`、`rare`、`epic`、`legendary` の4段階とする。
- 低レアはシンプルな円、中レアは盾型と星、高レアは光る枠と複数装飾、最高レアは虹色とオーラを使う。
- 図鑑のしょうごうタブ、名前の下の肩書き横、UFO窓の小バッヂは同じエンブレム部品を使う。

## デバッグメニュー保護

- 設定画面のバージョン番号5回タップは維持するが、その後に固定パスワード入力を求める。
- 正しいパスワードを入力したときだけ、かいはつしゃメニューを開く。
- バージョン表示の近くに「タップしたら開く」などの案内文は表示しない。
- パスワードは `kukucchi-dev` とする。
- フルオープンは、ショップ/カスタム品、UFO、なかま、称号、エフェクト、カギ、宝箱コレクション、ボス限定おたから、低学年モンスター、高学年モンスター、ボス進捗を確認用に解放する。

## すべてをしるもの

- 称号 `すべてをしるもの` は真の最終称号とする。
- 取得条件は、次の4条件をすべて満たすこと。
  - `すべてをしるもの` 以外の全称号を取得している。
  - カスタム対象の全アイテムを所持している。対象は、はいけい、UFO、ぼうし、スーツ、なかま、エフェクト。
  - 全モンスターを集めている。対象は、低学年81体と高学年20体。
  - 全ボスをげきムズまでクリアしている。
- 条件判定では `すべてをしるもの` 自身を必要称号から除外し、循環条件を作らない。
- 条件を満たした瞬間に、最高レアの特別エンブレム付きで付与する。
- 全げきムズ達成時のスペシャルUFOと `ぎんがのうず` は従来どおり付与するが、`すべてをしるもの` は上記4条件がそろうまで付与しない。

## SaveData

- 本フェーズではSaveDataバージョンを変更しない。
- デバッグパスワードの成否やメニューを開いた状態はSaveDataへ保存しない。

# 25.24 Phase B1 たしざんの星 playable core

## 星マップ

- ホーム画面に `けいさんのほし` の星マップを置く。
- `かけざんのほし`、`たしざんのほし`、`ひきざんのほし` は `live` とする。
- `たしざんのほし` は緑を主色、暖色をアクセントにし、既存の `かけざんのほし` の寒色テーマと被らないようにする。
- `たしざんのほし` から `おぼえる` へ入り、エリア選択、問題ループ、結果画面まで進める。

## たしざん6エリア

- たしざんのエリア定義は `src/data/planets.ts` の `additionAreas` に置く。
- `GeneratorSpec` はエリアごとに `operation: 'addition'`、`areaId`、`rule`、`category`、最小/最大の加数を持つ。
- エリアは次の6つとする。
  - `1〜9のたしざん`: `a,b` は1〜8、`a+b <= 9`。答え9以下の入門エリアとして先頭に置く。
  - `10までのたしざん`: `a,b` は1〜9、`a+b <= 10`。
  - `くりあがりのたしざん`: `a,b` は1〜9、`a+b >= 11`。
  - `2けたのたしざん`: `a,b` は10〜99、一の位と十の位がどちらも繰り上がらない。
  - `2けたのたしざん（くりあがり）`: `a,b` は10〜99、一の位の和が10以上。
  - `大きいかずのたしざん`: `a,b` は100〜999。
- くりあがりのたしざんは後続フェーズでボスを強くできるよう、独立エリアとして維持する。

## 問題生成と4択

- たしざん問題生成は `src/game-engine/questions/addition.ts` に置く。
- 問題IDは `add:{areaId}:{left}+{right}` とし、九九の `leftxright` と混ざらないようにする。
- たしざんのB1入力方式は4択に統一する。
- ダミー選択肢は、繰り上がり忘れ、`+10/-10`、`+1/-1`、数字の入れ替えなど、近い誤答から作る。
- `おぼえる` 画面の問題表示、正誤判定、フィードバック、次問題、結果遷移は既存の `LearnPage`、`AnswerControls`、`isCorrectAnswer`、`buildSessionSummary` を流用する。

## 報酬・がっこうモード・にがて

- 正解時のコイン・EXPは既存の `buildSessionSummary` と `applySessionResult` で付与する。
- がっこうモード2の報酬テーパリングは、たしざんIDも `progress.facts` から読んで適用する。
- 適応出題は `selectAdaptiveAdditionFact` で、既存の未マスター優先、にがて優先、2連続ミス時の易化重みを流用する。
- にがて記録は既存の `progress.facts` に保存する。たしざんは `operation: 'addition'` と `areaId` を持ち、九九と区別する。
- B1では、たしざんのにがて克服を新モンスター、なかま、図鑑報酬には接続しない。九九モンスター報酬は従来どおり九九IDだけを対象にする。
- ボス解放の段別正解数は、九九IDだけを集計し、たしざんの `left` 値を段として扱わない。

## SaveData

- 本フェーズではSaveDataバージョンを変更しない。
- たしざん進捗は既存の `progress.facts` と `categoryCorrect` を使い、追加のトップレベル保存フィールドは作らない。
- 旧セーブのマイグレーションは既存処理を維持し、たしざん fact を古い九九モンスター補正の対象にしない。

## B2以降に送るもの

- たしざん専用モンスター、ボス、UFO、エフェクト、称号は追加しない。
- `あそぶ`、`すぴーど`、ロケット等のたしざん対応はB2以降とする。
- ひきざんの星はB3でたしざんと対称の構造として実装する。

# 25.25 Phase N 星さいしょナビ

## ナビ構造

- ホームは「ほしをえらぶ」を主役にし、左から `たしざんのほし`、`ひきざんのほし`、`かけざんのほし` の順に選ばせる。
- `かけざんのほし`、`たしざんのほし`、`ひきざんのほし` はliveとして表示する。
- 星を選んだ後に、その星のメニュー画面へ進む。ホームから `あそぶ`、`おぼえる`、`スピード`、`高学年` へ直接入る大カードは置かない。
- 共通機能の `カスタム`、`図かん`、`ショップ`、`せってい` は、星を選ぶ前のホームから常に入れる。
- 旧 `/games` は互換用に `かけざんのほし` メニューへ送る。

## 星メニュー

- 星メニュー上部に、UFO窓（くくっち号）、Lv/EXP/コイン、今日のミッション、装備、にがてを配置し、ホームから移した情報が消えないようにする。
- `かけざんのほし` には、`あそぶ`、`おぼえる`、`スピード`、`高学年` を置く。既存の `にがてもんすたー`、`ぼすばとる`、`ろけっと`、`たからばこ` も、かけざんのほし配下に置く。
- `高学年` は、かけざんのほしメニューにだけ置き、平方数・円周率・発展カテゴリへ進める入口にする。
- `たしざんのほし` には、B1.1で整理した6エリアの `おぼえる`、`あそぶ`、`スピード`、`たしざんボス` を接続する。

## スコープ

- 問題生成、報酬、カスタム、図鑑、ショップ、セーブ内容は変更しない。
- SaveDataバージョンは変更しない。
- 画面サイズ 360x640、390x844、768x1024、1024x768 で、星選択、星メニュー、移設したUFO窓/ステータス/ミッションが見切れないようにする。

# 25.26 Phase B1.1 たしざん調整とカスタム星フィルタ

## 星の並び順

- ホームの「ほしをえらぶ」は、学年順に左から `たしざんのほし`、`ひきざんのほし`、`かけざんのほし` とする。
- `たしざんのほし`、`ひきざんのほし`、`かけざんのほし` はliveとする。

## たしざんのおぼえる

- `たしざんのほし` から `おぼえる` に入った場合、九九、平方数、円周率の切り替えボタンは表示しない。
- たしざんの星では、たしざんエリア選択だけを表示する。
- 九九、平方数、円周率は `かけざんのほし` 側の `おぼえる` または `高学年` 導線に置く。

## たしざん6エリア

- `1〜9のたしざん` を先頭に追加する。`a,b` は1〜8、`a+b <= 9` とする。
- 既存の `10までのたしざん`、`くりあがりのたしざん`、`2けたのたしざん`、`2けたのたしざん（くりあがり）`、`大きいかずのたしざん` は残す。
- `1〜9のたしざん` と `10までのたしざん` は範囲が近いが、前者は答え9以下の入門、後者は10ちょうどを含む練習として分ける。

## カスタム星フィルタ

- カスタム画面に `ぜんぶ`、`たしざん`、`ひきざん`、`かけざん` の星フィルタを置く。
- フィルタは分類表示のためだけに使う。装備は1つのくくっち号に対する全星共通の見た目として保存する。
- 既定は `ぜんぶ` とし、全アイテム、UFO、なかま、エフェクト、称号を表示する。
- `かけざん` フィルタでは、既存の九九由来のUFO、にがて克服モンスター、ボス/宝箱/特別報酬エフェクト、称号を表示する。
- ショップ購入可能なカスタム品、スーツ、ショップなかま、ショップエフェクトは星に紐づかない共通入手として `ぜんぶ` にだけ表示する。
- `たしざん` と `ひきざん` は、専用報酬が追加された星のアイテムを表示する。B3以降、ひきざん由来アイテムは `origin: 'sub'` として `ひきざん` フィルタに出す。

## SaveData

- 入手元タグは `src/game-engine/custom/customInventory.ts` の静的分類で付与する。
- 既存セーブへ新フィールドは追加しない。SaveDataバージョンは変更しない。

# 25.27 Phase 15.8 星フィルタ分類修正とスクロール領域分離

## カスタム星フィルタの分類

- カスタムの入手元タグは、表示中の星フィルタとは別の `CustomRewardOrigin` として扱う。
- 表示フィルタは現時点では `ぜんぶ`、`たしざん`、`ひきざん`、`かけざん` の4つだけを出す。
- 入手元タグは `all`、`add`、`subtract`、`multiply` に加え、将来の `divide`、`decimal`、`fraction` を追加しやすい型にする。将来タグはUIにはまだ出さない。
- `かけざん` では、九九プレイ由来のUFO、にがて克服なかま、九九プレイ/ボス/特別報酬の称号・エフェクトを表示する。
- `ぜんぶ` では、全アイテムを表示する。ショップ購入可能な背景、ぼうし、スーツ、ショップなかま、ショップエフェクトは共通入手として `ぜんぶ` にだけ表示する。
- フィルタ変更後、現在タブが空になる場合は、そのフィルタ内で最初に項目があるタブへ移す。
- 装備はフィルタに関係なく全星共通のくくっち号に保存する。

## カスタム画面のスクロール

- デスクトップ幅では、カスタム画面を左のプレビュー/なまえアイコン領域と右のもちもの領域に分ける。
- 左のプレビュー/なまえアイコン領域はスクロールさせず、右の星フィルタ、タブ、タブ見出しを固定したまま、アイテムグリッドだけを縦スクロールする。
- スマホ幅では縦積みにし、もちもの領域内では星フィルタとタブが見えたまま、アイテムグリッドだけをスクロールする。

## 図かん画面のスクロール

- 図かんの全体進捗、タブ、タブ別進捗は固定パネルとしてまとめる。
- 固定パネルの下に `book-scroll-area` を置き、各タブのカードグリッドだけを縦スクロールする。
- 360x640、390x844、768x1024、1024x768 の画面サイズで、タブと進捗がカード一覧のスクロールに流されないようにする。

## SaveData

- 入手元タグは静的定義側で付与するため、既存セーブへ新フィールドは追加しない。
- SaveDataバージョンは変更しない。

# 25.28 Phase B2 たしざんの星ご褒美一式

## たしざんのなかま

- たしざんの星には6エリア x 3体、合計18体のたしざんモンスターを追加する。
- 1〜9のたしざんは `たすくん` 系、10までのたしざんは `とーたす` 系、くりあがりのたしざんは `くりあがりん` 系、2けたのたしざんは `にけたん` 系、2けたのたしざん（くりあがり）は `くりくり` 系、大きいかずのたしざんは `おおたす` 系とする。
- 図かんのモンスタータブには、既存のていがくねん/こうがくねんブロックと並列に `たしざんのなかま` ブロックを置く。
- カスタムのなかまタブにも、所持条件を満たしたたしざんモンスターを `origin: 'add'` のなかまとして表示する。

## たしざんボス

- 各エリアに1体ずつ、合計6体のたしざんボスを置く。
- ボスは `たすたすキング`、`とーたすプリンス`、`くりあがりマスター`、`にけたバロン`、`くりくりエンペラー`、`おおたすジェネラル` とする。
- 解放条件は各エリア累計20問正解とし、既存のボスバトルの正誤判定、難易度、クリア記録、称号付与の仕組みに乗せる。
- `くりあがりマスター` と `くりくりエンペラー` は、たしざん内の山場として強めに設定する。

## UFO、エフェクト、称号

- たしざん専用UFOとして、`プラスリング号`、`ダブルドーム号`、`サンライズ号` を追加する。ボス報酬なので `origin: 'add'` とする。
- たしざん専用エフェクトとして、ショップ販売の `あつまるひかり` と、ボス報酬の `プラスバースト` を追加する。ショップ販売は `origin: 'all'`、ボス報酬は `origin: 'add'` とする。
- たしざん称号として、`たしざんのたまご`、`たしざんビギナー`、`くりあがりファイター`、`たしざんマスター`、`くりあがりちょうじん`、`たしざんレジェンド` を追加する。たしざんプレイやたしざんボス由来の称号は `origin: 'add'` とする。
- カスタムの `たしざん` フィルタでは、これらのたしざん由来UFO、なかま、エフェクト、称号を表示する。

## あそぶとスピード

- たしざんの星メニューの `あそぶ` と `スピード` をliveにする。
- `あそぶ` はロケット/ミニゲームの既存ループを使い、たしざんの6エリアから問題を生成する。
- `スピード` は既存のタイムチャレンジUIを使い、たしざんエリアを選んで出題できるようにする。
- がっこうモード、報酬テーパリング、にがて記録、経験値/コイン付与は、B1で一般化した既存システムを引き続き使う。

## アイコンプレビュー

- 設定画面のレベル解放アイコンは、選択中や一覧表示で画面を圧迫しないよう、プレビューに最大幅/高さを設定する。

## SaveData

- 新コンテンツの入手元タグは静的定義で持つ。
- たしざんモンスターの所持判定は既存の `progress.categoryCorrect` と `progress.facts` から導出する。
- 新しいトップレベル保存フィールドは追加しない。SaveDataバージョンは変更しない。

# 25.29 Phase B2.1 UFO座標再修正とたしざん調整

## UFO座標の共通化

- くくっち号の表示は、`KukucchiCharacter` と `getHomeShipPreviewVisuals` / `getHomeShipVisuals` を中心に使う。
- ホーム、星メニュー、カスタム、開始画面、スピード小窓などで、`.ufo-ship` の `top`、`width`、`height` を画面別に独自計算しない。
- UFO本体、円盤、ドーム、なかまの位置関係は、CSS変数による共通比率で決める。
- Phase N/B2で追加したホーム/星メニュー側のCSS上書きが、Phase 15.7の共通座標から漏れないようにする。

## たしざんの星の文言

- たしざんの星で見えるエリア名、モード名、ボス名、モンスター名、UFO名、エフェクト名、称号、説明文は、1年生向けにひらがな中心にする。
- たしざんルートで見える `入力` は `にゅうりょく`、`エリア` は `えりあ`、`ボス` は `ぼす`、`ロケット` は `ろけっと`、`スピード` は `すぴーど` とする。
- かけざんの星、高学年、既存のかけざん報酬名は本フェーズでは変更しない。

## たしざんロケット

- たしざんの星の `あそぶ`（ろけっと）には、`やさしい`、`ふつう`、`むずかしい` の3難易度を置く。
- `やさしい` は1けたのたしざん、`ふつう` は2けたまでのたしざん、`むずかしい` は3けたまでのたしざんを出題する。
- 難易度クリア称号として、`たしざんロケットビギナー`、`たしざんロケットパイロット`、`たしざんロケットキャプテン` を追加し、`origin: 'add'` とする。
- かけざんのロケット難易度は変更しない。

## レベルアイコン

- 初期のなまえアイコン `たまご`、`ほし`、`はな`、`そら` は絵文字ではなくSVGで描く。
- レベル解放アイコンと同じ表示枠、同じ最大サイズで、設定画面、カスタム画面、ホーム、解放演出に表示する。

## デバッグフルオープン

- フルオープンは、全ショップ品、全UFO、全称号、全ボス記録に加えて、たしざんモンスター18体も解放状態にする。
- たしざんモンスターは保存フィールドを増やさず、既存の `progress.categoryCorrect['addition:<areaId>']` を閾値以上にして解放状態を導出する。

## ボスバトルのもどる

- かけざん基本ボスは `/battle` 上でバトル状態を持つため、実行中の戻るリンクが `/battle` を指すだけでは画面状態が戻らない。
- `/battle` 内ではトップのもどるボタンを画面内コールバックにし、難易度選択前、開始前、バトル中、結果画面からボス一覧へ戻せるようにする。
- `/boss/:bossId` で入る高学年/たしざんボスは、従来どおり星メニューや高学年メニューへのリンクで戻る。

## SaveData

- 新しいトップレベル保存フィールドは追加しない。称号名差し替えに伴い、SaveData v14で旧たしざんロケット称号を新称号名へ移行する。
- たしざんロケット称号は既存の `player.titles` と `collectionRecords` に保存する。

# 25.30 Phase 15.9 UFOデザイン修正とがっこうモード通知

## UFO共通ベース

- `KukucchiCharacter` のUFO表示は、共通CSS変数でドーム、体、円盤の比率を決める。
- くくっち本体はドームに対して大きめに表示し、カスタム画面と星メニュー画面で同じデフォルメ比率になるようにする。
- 円盤下部には、左・中央・右の3か所に小さな半円パーツを置く。
- 3半円は全UFOに共通で表示し、UFOごとの色、リング、ライトなどの差別化要素は維持する。

## たしざんロケット称号

- 旧称号 `ろけっとびぎなー`、`ろけっとぱいろっと`、`ろけっときゃぷてん` は使わない。
- 新称号は `たしざんロケットビギナー`、`たしざんロケットパイロット`、`たしざんロケットキャプテン` とし、`origin: 'add'` を維持する。
- SaveData v14で、既存の `player.titles`、`player.currentTitle`、`progress.collectionRecords` を旧称号から新称号へ移行する。

## がっこうモード通知

- 1日のじかんバジェット到達通知は、画面中央のモーダルとして表示し、背景に薄暗いオーバーレイを置く。
- 表記は `10ぷん たったよ`、`このあとは コインとけいけんちは たまらないよ`、`あそびと きろくは そのまま つづけられるよ` のように、全星共通で低学年にも読める表記にする。

## SaveData v14

- たしざんロケット称号の名称差し替えだけを移行する。
- 新しいトップレベル保存フィールドは追加しない。

# 25.31 Phase B3 ひきざんの星一式

## ひきざんの星

- `ひきざんのほし` はliveとし、ホームの星選択から選べるようにする。
- テーマは `ゆうやけのくれーたー` とし、夕焼け系の色でたしざん・かけざんと区別する。
- ひきざんの星で見えるエリア名、モード名、ボス名、モンスター名、称号、説明、ボタンは、1年生向けにひらがな中心の表記にする。

## ひきざん6エリア

- `src/data/planets.ts` の `subtractionAreas` で、たしざんの6エリアと同じ構造の `GeneratorSpec` を持つ。
- 問題生成は `operation: 'subtraction'`、`areaId`、`rule`、`category` で区別し、にがて記録の式IDは `sub:<areaId>:a-b` とする。
- すべてのエリアで `a - b >= 1` を保証し、答えが負または0になる問題は出さない。
- 1: `1〜9のひきざん` は `a=2〜9`、`b=1〜a-1`。
- 2: `10からのひきざん` は `a=2〜10`、`b=1〜a-1`。
- 3: `くりさがりのひきざん` は `a=11〜18`、一の位で借りる1桁のひきざん。
- 4: `2けたのひきざん` は2桁-2桁で各位が借りない。
- 5: `2けたのひきざん（くりさがり）` は2桁-2桁で一の位が借りる。
- 6: `大きいかずのひきざん` は3桁-3桁で、百から十、十から一へ波及するくりさがりも含める。

## 問題ループとがっこうモード

- ひきざんは、たしざんと同じ問題ループ、4択UI、正誤判定、結果画面、コイン/けいけんち付与を使う。
- ダミー選択肢は、くりさがり忘れ、±1、±10、±100、桁の入れ替えなど、ありがちな間違いを優先する。
- がっこうモードの時間バジェット、報酬テーパリング、未マスター優先、2連続ミスで易しくする適応選択は、ひきざん式にも適用する。
- にがて記録は `sub:` ではじまる式IDで、たしざん・かけざんと混ざらないようにする。

## ひきざんのごほうび

- ひきざんモンスターは6エリア x 3体の18体を追加し、図かんに `ひきざんのなかま` ブロックを置く。
- モンスターは `ひきっこ`、`てんから`、`くりさがりん`、`にけたひき`、`さがりん`、`おおひき` 系とし、欠ける、分かれる、下がる、マイナスのモチーフでたしざん・九九と差別化する。
- ひきざんボスは6体で、`ひきひききんぐ`、`てんからぷりんす`、`くりさがりますたー`、`にけたばろん`、`さがりえんぺらー`、`おおひきじぇねらる` とする。各エリア累計20問正解で挑戦できる。
- UFOは `まいなすりんぐごう`、`すぷりっとごう`、`さんせっとごう` の3種を追加し、Phase 15.9の共通ベースを使う。
- エフェクトはショップ販売の `ちるひかり`（`origin: 'all'`）と、ボス報酬の `まいなすふらっしゅ`（`origin: 'sub'`）を追加する。
- 称号は `ひきざんのたまご`、`ひきざんびぎなー`、`くりさがりふぁいたー`、`ひきざんますたー`、`くりさがりちょうじん`、`ひきざんれじぇんど`、`ひきざんろけっとびぎなー`、`ひきざんろけっとぱいろっと`、`ひきざんろけっときゃぷてん` の9種を追加する。
- ひきざん由来の新規アイテムはすべて `origin: 'sub'` を持ち、ショップ共通品だけ `origin: 'all'` とする。

## あそぶ、すぴーど、ろけっと

- ひきざんの星の `あそぶ`、`おぼえる`、`すぴーど` はすべてliveとする。
- `あそぶ` は既存のろけっと/ミニゲームの流れを使い、ひきざん用の3難易度を置く。
- `やさしい` は1けた、`ふつう` は2けたまで、`むずかしい` は3けたまでのひきざんを出す。
- 各難易度をクリアしたとき、対応するひきざんろけっと称号を付与する。

## カスタム、図かん、フルオープン、SaveData

- カスタムの星フィルタでは `ひきざん` を `origin: 'sub'` として扱い、ひきざんUFO、ボス報酬エフェクト、ひきざん称号、ひきざんのなかまを表示する。
- 図かんのモンスター進捗には、九九、たしざんに加えてひきざんモンスター18体を含める。
- デバッグのフルオープンは、動的なアイテム列挙と `progress.categoryCorrect` の補完で、ひきざんのごほうびも解放する。
- 新しいトップレベル保存フィールドは追加しない。既存の `progress.facts`、`progress.categoryCorrect`、`collectionRecords`、所持アイテム定義で表現するため、SaveDataバージョンは据え置く。

# 25.32 Phase B4 わりざんの星一式

## わりざんの星

- `わりざんのほし` はliveとし、ホームの星選択から選べるようにする。
- テーマは青紫系の `むらさきネビュラ` とし、たしざん、ひきざん、かけざんと色で区別する。
- わりざんは3年生中心の星なので、かけざんと同じく漢字を含む表記を許可する。

## わりざん3エリアとあまり型

- `src/data/planets.ts` の `divisionAreas` で、3つの `GeneratorSpec` を定義する。
- 問題生成は `operation: 'division'`、`areaId`、`rule`、`category`、`answerKind` で区別し、にがて記録の式IDは `divide:<areaId>:a/b` とする。
- 1: `あまりなしのわりざん` は、割る数1〜9、商1〜9の九九の逆で、答えは整数とする。
- 2: `あまりのあるわりざん` は、割る数2〜9、商1〜9、あまり1以上かつ割る数未満の問題を出し、答えは商とあまりのセットとする。
- 3: `大きいかずのわりざん` は、2桁÷1桁で、割り切れる問題とあまりありの問題を混ぜる。
- `AnswerValue` は、既存の整数/文字列に加えて `{ kind: 'remainder', quotient, remainder }` を扱う。あまり0は整数として表示し、`4あまり0` とは表示しない。

## あまりの4択UIと判定

- 4択UIは既存の `AnswerControls` を使い、あまりありの選択肢は `3あまり1` のように商とあまりを1セットで表示する。
- 正誤判定は `answerValueKey` で正規化し、商とあまりが両方一致したときだけ正解にする。
- ダミー選択肢は、商が1ずれる、あまりが1ずれる、商とあまりの入れ替え、あまりが割る数以上になる誤答を優先する。
- 問題ループ、結果画面、コイン/けいけんち付与、がっこうモードの時間バジェット、報酬テーパリング、未マスター優先、2連続ミスで易しくする適応選択は、既存の仕組みを流用する。

## わりざんのごほうび

- わりざんモンスターは3エリア x 4体の12体を追加し、図かんに `わりざんのなかま` ブロックを置く。
- モンスターは `わりっこ`、`あまりん`、`おおわり` 系とし、等分、分割、配る、あまりのモチーフで他の星と差別化する。
- わりざんボスは3体で、`わりわりキング`、`あまりマスター`、`おおわりエンペラー` とする。各エリア累計20問正解で挑戦できる。
- UFOは `ディバイドリング号`、`クォーター号`、`ネビュラ号` の3種を追加し、Phase 15.9の共通ベースを使う。
- エフェクトはショップ販売の `わけるひかり`（`origin: 'all'`）と、ボス報酬の `ディバイドフラッシュ`（`origin: 'divide'`）を追加する。
- 称号は `わりざんのたまご`、`わりざんビギナー`、`あまりファイター`、`わりざんマスター`、`あまりはかせ`、`わりざんレジェンド`、`わりざんロケットビギナー`、`わりざんロケットパイロット`、`わりざんロケットキャプテン` の9種を追加する。
- わりざん由来の新規アイテムはすべて `origin: 'divide'` を持ち、ショップ共通品だけ `origin: 'all'` とする。

## あそぶ、スピード、ロケット

- わりざんの星の `あそぶ`、`おぼえる`、`スピード` はすべてliveとする。
- `あそぶ` は既存のロケット/ミニゲームの流れを使い、わりざん用の3難易度を置く。
- `やさしい` はあまりなし、`ふつう` はあまりあり、`むずかしい` は大きいかずのわりざんを出す。
- 各難易度をクリアしたとき、対応するわりざんロケット称号を付与する。

## カスタム、図かん、フルオープン、SaveData

- カスタムの星フィルタでは `わりざん` を `origin: 'divide'` として扱い、わりざんUFO、ボス報酬エフェクト、わりざん称号、わりざんのなかまを表示する。
- 図かんのモンスター進捗には、九九、たしざん、ひきざんに加えてわりざんモンスター12体を含める。
- デバッグのフルオープンは、動的なアイテム列挙と `progress.categoryCorrect` の補完で、わりざんのごほうびも解放する。
- 新しいトップレベル保存フィールドは追加しない。既存の `progress.facts`、`progress.categoryCorrect`、`collectionRecords`、所持アイテム定義で表現するため、SaveDataバージョンは据え置く。

# 26. 完了報告の形式

この報告は、Phase 2完了時の停止時、および以降の各セッション終了時に行ってください。

## 実装した内容

主要機能を箇条書きで説明。

## 主要ファイル

追加・変更した主要ファイルと役割を説明。

## データ設計

保存データ、習熟度、問題生成の仕組みを説明。

## テスト結果

実行したコマンドと結果を記載。

## 動作確認

確認できた画面と操作を記載。

## 人間によるレビュー依頼

`src/data/kukuReadings.ts` の読み仮名など、人間の最終確認が必要な箇所を列挙。

## 未実装・制限事項

未実装機能や簡略化した部分を正直に記載。

## 次に実装すべき機能

優先順位を付けて提案。

---

# 27. 最重要指示

まずは見た目だけのモックではなく、以下の一連の体験が完全に動く状態を優先してください。

```text
初回設定
↓
ホーム
↓
ゲーム選択
↓
問題に回答
↓
正解演出
↓
コイン・経験値獲得
↓
リザルト
↓
称号または自己ベスト
↓
ホームへ戻る
↓
進捗が保存されている
```

このコアループ（＝Phase 2）が完成したら、そこで停止して報告してください。ミニゲームや高学年モードの拡張は、次の指示で進めます。

作業を開始してください。
